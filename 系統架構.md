# EQ Performance Dashboard — 系統架構說明 (系統架構.md)

> 給 AI 的 Context：本文件描述 **EQDashboard.V2.Web**（重構版、目前主線）的完整系統架構、目錄樹與資料流向。
> 開發時請參考此份文件以理解各模組與檔案的職責劃分。

---

## 1. Tech Stack (核心技術棧與版本)

- **後端框架**：ASP.NET Core 9.0 MVC + Static Files (Host 於 Kestrel / IIS)
- **資料庫與 ORM**：MSSQL Server + EF Core 9 + 原生 ADO.NET (`Microsoft.Data.SqlClient`)
- **前端核心**：純 ES Modules 架構 (Vanilla JS) + jQuery + Bootstrap 5 + DataTables。無 Webpack/Vite 等 Bundler，全數走 CDN。
- **身分驗證**：Windows Negotiate 自動偵測 (無手動帳密表單)。
- **Schema 管理**：無 EF Migrations，由 `SchemaBootstrap` 於啟動時執行 Idempotent Raw SQL 自我修復與建表。

---

## 2. Directory Structure (專案目錄結構樹)

```text
EQDashboard.V2.Web/
├── EQDashboard.V2.Web.sln              # 方案檔：含 Web 專案 + 巢狀整合測試專案
├── EQDashboard.V2.Web.csproj           # 目標 .NET 9，Nullable/ImplicitUsings enable
├── EQDashboard.V2.Web.Tests/           # 巢狀整合測試 (xUnit + WebApplicationFactory)
├── Program.cs                          # 進入點：DI 註冊 + Middleware Pipeline + 健康檢查
├── appsettings.json                    # 連線字串 (EQDashboard) 與 AuthSettings 設定
├── Controllers/                        # 薄 Controller：定義路由與 API 端點，邏輯委派給 Service
├── Services/                           # 核心業務邏輯 (MenuService, AccountService 等)
├── Models/                             # 實體類別 (Entities)，每表一檔
├── Data/                               # AppDbContext 與 Entity Configurations (複合 PK/FK 設定)
├── Middleware/                         # 自訂中介軟體 (ActivityLoggingMiddleware 等)
└── wwwroot/                            # 靜態前端資源
    ├── index.html                      # 唯一 HTML 進入點；也是唯一的 <script type="module"> 入口
    │                                   #   （只載 main.js，其餘 20 支模組全由 main.js 的 import 圖帶入；
    │                                   #    模組 import 一律不帶 ?v=，防同檔被載成兩個模組實例）
    ├── partials/modals.html            # 所有 Bootstrap Modal 集中定義，由 fetch 動態載入
    ├── css/                            # 樣式表 (variables, navbar, sidebar, responsive)
    ├── js/                             # ES Modules 前端程式碼
    │   ├── main.js / store.js / api.js # 核心初始化、全域狀態、API 攔截器與封裝
    │   ├── ui/                         # 版面與互動 (layout, navigation, dialogs)
    │   ├── render/                     # 畫面渲染 (sidebar, tables, account-ui)
    │   └── admin/                      # 後台管理功能 (menu-manage, role-manage 等)
    └── images/icons/                   # 圖示實體檔 (DB 只存路徑)
```

---

## 3. Core Modules (核心元件與模組功能)

### 後端核心模組
- **`SettingsService` (快取與全域資料)**：雙層快取（Global 60s / Volatile 10s）。負責供應 `GetInitialData` API 所需的全站基礎設定。
- **`MenuService` & `MenuAuthService` (選單與權限)**：處理看板的 CRUD、拖曳排序、樹狀結構重建。AuthService 負責依照「Menu ACL > Account override > Role」優先序產生使用者可視清單。
- **`AccountService` (帳號管理)**：處理數萬筆帳號的 CRUD，含 soft-delete backup 與 Server-side 分頁查詢。
- **`SchemaBootstrap` (DB 守門員)**：啟動時自動檢查並補齊缺失的 Data Tables、Columns 與 Indexes，確保程式與 DB 結構同步。
- **`ActivityLogProcessor` (非同步稽核)**：利用 `Channel<T>` 接收前端操作紀錄，於背景批次 (BackgroundService) 寫入 DB 以降低 API 延遲。

### 前端核心模組
- **`store.js` (`appState`)**：前端狀態管理中心，收斂所有的全域變數（currentUser, validMenus 等）。
- **`api.js`**：所有 DB 讀寫的閘口。內建 `fetch` 攔截器，負責自動帶入 `X-Requested-With` 標頭與 CSRF Token 重試機制。
- **`ui/layout.js` & `render/sidebar.js`**：控制左側欄與上方導覽的響應式展開/收合。Sidebar 採用 App Shell 快取機制，利用 LocalStorage 在重新整理時達到「零閃爍」載入。
- **`admin/menu-manage.js`**：看板網頁管理的複雜邏輯，包含樹狀結構的拖曳建構 (`buildTreeUI`) 與批次存檔。

---

## 4. Data Flow / State Management (資料流向與狀態管理)

### API 資料請求與狀態流向
1. **Request (瀏覽器端)**：所有前端對後端的 API 呼叫統一經過 `api.js`。攔截器會自動附加 CSRF Token。
2. **Controller -> Service**：Controller (`Controllers/`) 只負責路由與參數驗證，隨即將操作委派給 Service (`Services/`)。
3. **Database (MSSQL)**：Service 透過 `AppDbContext` (EF Core) 或 原生 `SqlConnection` (ADO.NET) 對 DB 進行操作。
4. **Cache Invalidation**：一旦發生寫入操作：
   - 若走 EF Core，`CacheInvalidationInterceptor` 會自動攔截並呼叫 `InvalidateInitialDataCache`。
   - 若走 ADO.NET，開發者必須手動呼叫 `InvalidateInitialDataCache`。
   - 清除快取時會同步更新全域 `ETag`。
5. **Response (前端狀態更新)**：前端拿到 HTTP 200/204 後，會呼叫 `clearAppCache` 清除 LocalStorage 暫存，接著可能呼叫 `fetchInitialDataFromDB` 重新拉取最新的 `appState` 資料並重繪畫面。

### 特殊資料流設計
- **帳號表分頁**：為了因應未來十萬級別的帳號，`Accounts` 列表是全站唯一的 **Server-side DataTable** (`GET /api/Accounts?page=...`)。其餘的群組、廠區、看板列表則是一次拉回前端，交由 DataTables 進行 Client-side 分頁。
- **冪等登入統計**：使用者的登入統計不會阻塞登入流程。`UpdateLoginStats` 會以 `UPDATE ... IF @@ROWCOUNT=0 INSERT` 的單句 T-SQL 進行 `DailyUserVisits` 的 UPSERT 寫入，確保 Web Farm 多台主機同時請求也不會發生 PK 衝突。
- **個人設定隔離**：`PersonalSettings` (個人排序與捷徑) 是綁定 `EmpId` 的。這部分的資料不會進入全域共享快取，而是在 `GetInitialData` 時透過 EF Core Point Query 直接抓取登入者專屬的設定回傳。
