# EQ Performance Dashboard — 系統架構說明 (系統架構.md)

> 本文件描述 **EQDashboard.V2.Web**（重構版、目前主線）的完整目錄結構與各檔案職責。
> 用途：UMC（聯電）廠區效能看板入口 — 依使用者權限/廠區顯示可存取的看板連結，並提供帳號/權限/看板的後台管理。
> 開發規範與安全現況請對照 `CLAUDE.md`；現況快照與待辦請對照 `memory.md`；DB 結構與增量 SQL 歷史請對照 `DB_Table.md`。
> 最後更新：2026-07-19。

---

## 1. 專案概觀

| 項目 | 說明 |
| --- | --- |
| 框架 | ASP.NET Core 9.0 MVC + Static Files（Kestrel / IIS）|
| 資料存取 | EF Core 9 + 原生 ADO.NET（`Microsoft.Data.SqlClient`）|
| 資料庫 | MSSQL（Server `Sariel`，Catalog `EQDashboardV2`，User `testuser`）|
| 前端 | 純 ES Modules（無 bundler）＋ jQuery + Bootstrap 5 + DataTables（全走 CDN）|
| 身份驗證 | Windows Negotiate 自動偵測（`AuthSettings`：`SimulatedAccount` 模擬 / `DefaultAdmins` 防鎖死 / `OpenAccessMode` 開放瀏覽）；前端無手動帳密 Tab，LDAP 手動登入僅保留後端能力與測試用 |
| Schema 管理 | **無 EF Migrations**；由 `SchemaBootstrap` 啟動時 idempotent raw SQL 自我修復 |
| 資料保存 | CRUD 異動自動靜默寫回 MSSQL（無「同步至 DB」按鈕）|

> 另有舊版 **EQDashboard**（單一專案、JS/CSS 單檔），僅作參考，非主線。兩版版面與功能 100% 一致。

---

## 2. 目錄結構樹

```
EQDashboard.V2.Web/
├── EQDashboard.V2.Web.sln              # 方案檔（現役唯一）：含 Web 專案 + 巢狀測試專案
├── EQDashboard.V2.Web.csproj           # 專案檔（.NET 9、Nullable/ImplicitUsings enable）；預設 glob 排除 EQDashboard.V2.Web.Tests\** 避免雙重編譯
├── EQDashboard.V2.Web.Tests/           # 巢狀最小整合測試（xUnit + WebApplicationFactory<Program>）：AuthzMatrixTests.cs 鎖 authz/CSRF/可見性矩陣
├── Program.cs                          # 進入點：DI 註冊 + middleware pipeline + 健康檢查端點
├── web.config                          # IIS 託管設定（AspNetCoreModuleV2）
├── appsettings.json                    # 連線字串 / Auth 設定（不進版控，有 .example 範本）
├── appsettings.Development.json        # 開發環境覆寫
├── appsettings.Production.json         # 正式環境覆寫
├── package.json / package-lock.json    # 僅供開發工具（如 node --check）；前端執行期不依賴 npm
├── Properties/
│   └── launchSettings.json             # 啟動 profile（http 5242 / https 7033;5242 / IIS Express）
│
├── Controllers/                        # 薄 Controller：路由 + 回傳，邏輯委派 Service 或留 CRUD
├── Services/                           # Service 層 + Interfaces/
├── Models/                             # 每個 Entity 一檔 + Settings/
├── Data/                               # AppDbContext + Configurations/
├── Middleware/                         # 自訂 middleware
├── Helpers/                            # 共用工具
└── wwwroot/                            # 靜態前端
    ├── index.html                      # 唯一進入點 <script type="module" src="js/main.js">
    ├── partials/modals.html            # Bootstrap Modal 集中檔，由 fetch 動態載入
    ├── css/                            # 切分後的樣式
    ├── js/                             # ES Modules（root + ui/ + render/ + admin/）
    ├── images/icons/                   # 圖示實體檔（DB 只存路徑 /images/icons/{guid}.{ext}）
    └── tools/install-ie-protocol.reg   # 「另開分頁(IE)」客戶端 ie: 協定安裝檔（一次性匯入/GPO 派送）
```

> **SQL 腳本位置**：增量 DB 異動腳本放在**方案根目錄** `sql\`（如 `sql\2026-07-18_Add_DailyUserVisits.sql`），與 `DB_Table.md` Changelog 一一對應；完整結構快照見 `DB_Table.md`。（舊專案內 `sql/` 目錄與 `schema_v2.sql` 等腳本已移除。）

---

## 3. 後端檔案逐一說明

### 3.1 進入點與設定

| 檔案 | 職責 |
| --- | --- |
| `Program.cs` | 應用組裝中樞：註冊 DI（所有 Service/DbContext/MemoryCache/Antiforgery/ResponseCompression/RateLimiter/Negotiate）、設定 middleware pipeline（驗證→授權→ActivityLogging→StaticFiles→MVC）、啟動時呼叫 `SchemaBootstrap` 與 `MigrateBase64IconsAsync`、定義 `/health` 與 `/health/ready` 健康檢查端點。|
| `EQDashboard.V2.Web.csproj` | 目標 .NET 9；`Nullable`/`ImplicitUsings` 皆 enable；NuGet 套件（EF Core 9、Microsoft.Data.SqlClient、Swashbuckle 等）。|
| `web.config` | IIS 部署用；設定 AspNetCoreModuleV2 與 Windows 驗證（修過 500.19）。|
| `appsettings*.json` | 連線字串 key 一律 `ConnectionStrings:EQDashboard`（可用環境變數 `ConnectionStrings__EQDashboard` 覆寫）；`Auth` 區塊含 LDAP 設定與離線開發用 `TestAccounts`（正式環境須關閉）。|
| `Properties/launchSettings.json` | 本機啟動 profile 與連接埠。|
| `package.json` / `package-lock.json` | 開發輔助（前端走 CDN，**非** build pipeline）。|

### 3.2 Controllers/（11 個，皆 `[ApiController]`，DTO 定義於各檔末尾）

| 檔案 | 路由 / 職責 | 授權 |
| --- | --- | --- |
| `SettingsController.cs` | Legacy 全量 API：`GetInitialData`（一次取全部表，非 admin 後端按可見性 `FilterTable` 列級過濾；**帳號相關表 Accounts/PersonalSettings/Map_Account_* 連 admin 也收斂成「只回自己一列」**，全部帳號改走 `/api/Accounts`）、`SaveData`（全量 DELETE→INSERT 包交易，僅 Excel 匯入用）、`UpdateLoginStats`。支援 ETag/304。|混合（GetInitialData 寬鬆、SaveData admin）|
| `AccountsController.cs` | 帳號 CRUD（委派 `IAccountService`）；`GET /api/Accounts?page=&pageSize=&q=`（server-side 分頁清單，回 `{items,total,page,pageSize}`）、`GET /api/Accounts/{id}`（單帳號明細 lazy-load）、`GET /api/Accounts/export`（全量明細供 Excel 匯出，literal route 優先序高於 `{id}`）；刪除帶 soft-delete backup + 稽核。DTO：`AccountFullDto`。| `[Authorize(Roles="admin")]` |
| `MenusController.cs` | 薄 Controller，看板 CRUD + 批次（`BatchUpdateMenus`/`BatchDeleteMenus`，拖曳排序走此）委派 `IMenuService`；逐節點 `IMenuAuthService` 權限檢查。| `[Authorize]`（逐節點檢查）|
| `RolesController.cs` | 權限群組 CRUD + `Map_Role_Menu` 維護（寫入前驗 `AllowedMenuIds` 存在）。DTO：`RoleDto`。| `[Authorize(Roles="admin")]` |
| `FabsController.cs` | 廠區 CRUD + `Map_Fab_Role` 維護（寫入前驗 `AssignedRoles` 存在）。| `[Authorize(Roles="admin")]` |
| `AppsController.cs` | App 卡片 CRUD（圖示走 `IIconStorageService`）。| `[Authorize]`（逐節點檢查）|
| `AuthController.cs` | 登入流程：`WhoAmI`（Negotiate-challenge 偵測，擋未註冊不發 cookie）、LDAP 手動登入、登出、`CsrfToken` 核發。取 EmpId 走 `ClaimTypes.NameIdentifier`。| 寬鬆 baseline |
| `RequestsController.cs` | 申請單 CRUD（IDOR 防護：一般使用者只動自己的）。| `[Authorize]` |
| `PersonalSettingsController.cs` | 個人版面（上方導覽列）per-user delete+insert；強制 claim EmpId。| `[Authorize]` |
| `ActivityLogsController.cs` | 操作紀錄查詢（分頁）+ 清除。| `[Authorize(Roles="admin")]` |
| `AnalyticsController.cs` | 流量統計：`GET /api/Analytics/UsageStats?days=N`（DAU/MAU/註冊數/活躍率 KPI、每日與 12 個月趨勢、部門/廠區活躍比率）、`GET /api/Analytics/details`（每日個人造訪明細分頁）。查詢 `DailyUserVisits`。| `[Authorize(Roles="admin")]` |

### 3.3 Services/ + Services/Interfaces/

| 檔案 | 職責 |
| --- | --- |
| `SettingsService.cs` (`ISettingsService`) | 讀寫核心：`GetInitialDataAsync`（雙層 `IMemoryCache` — Global 60s / Volatile 10s，全部表載入成功才寫快取）、`SaveDataAsync`（全量覆寫包交易，FK 用 `WITH CHECK CHECK` 重驗、失敗 rollback）、`InvalidateInitialDataCache`/`InvalidateVolatileDataCache`（**雙重 load-bearing**：清快取 + bump 靜態 `_currentETag`）、`GetCurrentETag`。`TableNames` 為全量覆寫表清單（**不含** PersonalSettings）。另含 `UpdateLoginStats`（單語句 UPDATE+OUTPUT）→ `RecordDailyUserVisitAsync`（`UPDATE...IF @@ROWCOUNT=0 INSERT` 冪等 upsert `DailyUserVisits`，失敗僅 LogWarning 不阻斷登入）。|
| `AccountService.cs` (`IAccountService`) | 帳號 CRUD 邏輯：建/改/刪帳號與其 Role/委派/ACL mappings；刪除產生 soft-delete backup；每個寫入後 Invalidate（bump ETag）。讀取分三條：`GetAccountsPagedAsync`（清單，WHERE+Skip/Take 下推 DB、輕量欄位）、`GetAccountDetailsAsync`（單筆明細）、`GetAccountsForExportAsync`（全量明細，Excel 匯出）。|
| `AuthService.cs` (`IAuthService`) | 驗證邏輯：LDAP 帳密驗證、`TestAccounts` 比對；用 `IOptionsSnapshot<AuthSettings>`。|
| `MenuService.cs` (`IMenuService`) | 看板 CRUD/批次邏輯（由 `MenusController` 抽出，#82）：`BatchUpdateMenus` 結構重建（SortOrder/ACL）＋icon 孤兒清理＋委派閘、`BatchDeleteMenus`、單筆建改刪。多步寫入包交易（走 `CreateExecutionStrategy().ExecuteAsync`）、圖示走 `IIconStorageService`、寫入後 Invalidate。Scoped。|
| `MenuAuthService.cs` (`IMenuAuthService`) | 權限核心：`GetVisibleMenuIdsAsync`（可見看板集合，以 `visibleMenus:{ETag}:{empId}` 跨請求快取、回傳防禦性副本）、逐節點編輯權限檢查（走 `Map_Menu_Structure` parent chain）。優先序 **Menu ACL > Account override > Role**。Scoped。|
| `InitialDataCacheInvalidator.cs` (`IInitialDataCacheInvalidator`) | Singleton：集中「清 InitialData 快取（Global/Volatile）＋bump 靜態 `_currentETag`」職責，取代散落各 controller/service 的手呼。各寫入路徑注入此介面呼叫。|
| `CacheInvalidationInterceptor.cs` | EF Core `SaveChangesInterceptor` 安全網（Singleton）：偵測 `SaveChangesAsync` 動到權限相關實體（Menus/Map_Role_Menu/Map_Account_*/Map_Menu_Allow\|DenyAccount/Map_Menu_Structure/Fabs/Roles）即自動觸發 Invalidate，杜絕新寫入路徑漏呼叫。**raw ADO.NET 寫入（不走 EF tracking）不會被攔到，仍須手動 Invalidate**。AppDbContext `.AddInterceptors(...)` 掛載。|
| `IconStorageService.cs` (`IIconStorageService`) | 圖示存實體檔：`SaveAsync`（data: URI 依 MIME 白名單寫檔回路徑、URL 正規化、FA class 原樣）、`DeleteIfLocalUnreferencedAsync`（孤兒清理 + path-traversal 防護）、`MigrateBase64IconsAsync`（啟動一次性轉舊 base64）。|
| `SchemaBootstrap.cs` (`ISchemaBootstrap`) | 啟動時 idempotent raw SQL 自我修復 schema：補表/補欄位/補索引（含 `UserActivityLogs`、`DailyUserVisits` 自動建表）。**所有索引集中於 `EnsureIndexesAsync`**（`IF NOT EXISTS(sys.indexes) CREATE INDEX`）。|
| `ActivityLogger.cs` (`IActivityLogger`) | 稽核寫入入口：`LogAuditAsync`（組裝 `UserActivityLog` 丟入佇列）。|
| `ActivityLogQueue.cs` | Singleton `Channel<UserActivityLog>`；`FullMode=Wait`+`TryWrite` 非阻塞，滿載記 Warning **不丟最舊**；提供 `TryDequeue` 供批次 drain。|
| `ActivityLogProcessor.cs` | `BackgroundService`：`DequeueAsync` 等第一筆 + `TryDequeue` drain 成批（≤100）→ `AddRange` + 單次 `SaveChanges`；批次失敗退回逐筆隔離毒資料；每工作單位 `CreateScope()` 解析 Scoped DbContext。|
| `ActivityLogPurgeService.cs` | `BackgroundService`：每 24h（啟動延遲 5 分）讀 `ActivityLog:RetentionDays`，`>0` 則 `CreateScope()` 解析 `IActivityLogger.PurgeOlderThanAsync`（走 `ExecuteDeleteAsync`，不觸發 cache invalidation）刪更舊稽核；`<=0`/缺段＝停用。|

> Interfaces/ 下對應介面：`ISettingsService`/`IAccountService`/`IAuthService`/`IMenuService`/`IMenuAuthService`/`IInitialDataCacheInvalidator`/`IIconStorageService`/`ISchemaBootstrap`/`IActivityLogger`。（`CacheInvalidationInterceptor` 為具象 EF interceptor，無介面。）

### 3.4 Models/（每個 Entity 獨立一檔）

**實體表（7）**
| 檔案 | 對應表 / 重點欄位 |
| --- | --- |
| `Account.cs` | `Accounts`（含 `RoleLevel`/`CanEditOthers`/`LoginCount`/`LastLoginTime`）|
| `Menu.cs` | `Menus`（含 `Icon` 路徑字串）|
| `Fab.cs` | `Fabs`（廠區）|
| `Role.cs` | `Roles`（權限群組）|
| `AppItem.cs` | `Apps`（含 `IconBase64`，欄名不變但內容已是路徑）|
| `Request.cs` | `Requests`（申請單）|
| `PersonalSetting.cs` | `PersonalSettings`（複合 PK `EmpId`+`MenuId`，個人版面）|

**關聯表（10）**
| 檔案 | 對應表 |
| --- | --- |
| `MapFabRole.cs` | `Map_Fab_Role` |
| `MapAccountRole.cs` | `Map_Account_Role` |
| `MapAccountManageMenu.cs` | `Map_Account_ManageMenu`（委派）|
| `MapRoleMenu.cs` | `Map_Role_Menu`（含 `SortOrder`）|
| `MapMenuStructure.cs` | `Map_Menu_Structure`（父子，Restrict FK）|
| `MapAccountDefaultPage.cs` | `Map_Account_DefaultPage` |
| `MapAccountExtraMenu.cs` | `Map_Account_ExtraMenu`（override 加，**per-fab**：PK `(EmpId, FabId, MenuId)`，FabId 無 FK）|
| `MapAccountDenyMenu.cs` | `Map_Account_DenyMenu`（override 減，**per-fab**：PK `(EmpId, FabId, MenuId)`，FabId 無 FK）|
| `MapMenuAllowAccount.cs` | `Map_Menu_AllowAccount`（Menu ACL 白名單）|
| `MapMenuDenyAccount.cs` | `Map_Menu_DenyAccount`（Menu ACL 黑名單）|

**稽核 + 設定**
| 檔案 | 用途 |
| --- | --- |
| `UserActivityLog.cs` | `UserActivityLogs`（操作紀錄）|
| `DailyUserVisit.cs` | `DailyUserVisits`（每日活躍造訪統計，複合 PK `VisitDate`+`EmpId`）|
| `DailyMenuClick.cs` | `DailyMenuClicks`（每日看板點擊統計，複合 PK `ClickDate`+`MenuId`+`EmpId`）|
| `Settings/AuthSettings.cs` | 對應 `appsettings.json` 的 `Auth` 區塊（SimulatedAccount/DefaultAdmins/OpenAccessMode/LDAP/TestAccounts），由 `IOptions` 注入 |

> DTO（如 `AccountFullDto`/`RoleDto`/`MenuDto`/`AppDto`/`PersonalSettingDto`/`PagedResult`）**未獨立成 DTOs/ 資料夾**，而是定義在各對應 Controller 檔案末尾。

### 3.5 Data/

| 檔案 | 職責 |
| --- | --- |
| `AppDbContext.cs` | EF Core 上下文；用 `ApplyConfigurationsFromAssembly` 自動掃描所有 `IEntityTypeConfiguration`。|
| `Configurations/AccountConfiguration.cs` | `Account` 對應設定（NVARCHAR PK 等）|
| `Configurations/MenuConfiguration.cs` | `Menu` 對應設定 |
| `Configurations/FabConfiguration.cs` | `Fab` 對應設定 |
| `Configurations/RoleConfiguration.cs` | `Role` 對應設定 |
| `Configurations/AppItemConfiguration.cs` | `AppItem` 對應設定 |
| `Configurations/RequestConfiguration.cs` | `Request` 對應設定 |
| `Configurations/PersonalSettingConfiguration.cs` | `PersonalSetting` 複合 PK 設定 |
| `Configurations/MappingTablesConfiguration.cs` | 10 張關聯表的複合 PK / FK 設定 |
| `Configurations/UserActivityLogConfiguration.cs` | `UserActivityLog` 對應設定 |
| `Configurations/DailyUserVisitConfiguration.cs` | `DailyUserVisit` 複合 PK 設定 |
| `Configurations/DailyMenuClickConfiguration.cs` | `DailyMenuClick` 複合 PK 設定 |

> **索引不在此宣告**（EF `HasIndex` 對既有 DB 是 no-op）；索引單一事實來源＝`SchemaBootstrap.EnsureIndexesAsync`。

### 3.6 Middleware/ 與 Helpers/

| 檔案 | 職責 |
| --- | --- |
| `Middleware/ActivityLoggingMiddleware.cs` | 攔截請求、組裝稽核紀錄丟入 `ActivityLogQueue`（已跳過成功 GET 以降低寫放大）。|
| `Helpers/ClientIpHelper.cs` | `GetClientIp`：取 client IP（XFF 可偽造，**僅供稽核 log、不可用於權限判定**）。|

### 3.7 SQL 腳本（方案根目錄 `sql\`，手動維護、非自動執行）

| 檔案 | 用途 |
| --- | --- |
| `sql\2026-07-18_Add_DailyUserVisits.sql` | 增量：新增 `DailyUserVisits` 表與 `IX_DailyUserVisits_Date_Dept` 索引 |

> 完整建表快照與所有增量異動歷史（Changelog 只增不刪）＝ `DB_Table.md`；線上結構由 `SchemaBootstrap` 啟動時自動補齊。舊專案內 `sql/` 目錄（`schema_v2.sql` 等）已移除。

---

## 4. 前端檔案逐一說明（wwwroot/）

> ES Modules：`index.html` 唯一進入點，由 `main.js` `import` 整張模組圖。**所有 import 必須置頂**（任一模組 SyntaxError 會中止整張圖）。模組內 `function X` 需 `window.X = X` 才能給 HTML inline `onclick`；每檔的 `window.X=X` 曝露區塊只保留一份。

### 4.1 進入點與樣式

| 檔案 | 職責 |
| --- | --- |
| `index.html` | 唯一 HTML 進入點，只載入 `<script type="module" src="js/main.js?v=...">`；含 navbar / sidebar / 各 page section 容器。|
| `partials/modals.html` | 所有 Bootstrap Modal 集中於此，由 `fetch()` 動態載入插入 `#modals-container`，在 JS 初始化前完成。|
| `tools/install-ie-protocol.reg` | 「另開分頁 (IE)」（開啟方式 `target='ie'`）的客戶端協定安裝檔：註冊 `ie:` URL protocol → cmd 去掉前綴後 `start iexplore <URL>`。每台客戶端一次性匯入（或 GPO 派送）；未安裝時前端呼叫靜默無效。由 Program.cs 靜態檔 MIME 映射 `.reg`→`text/plain` 供下載。|
| `css/variables.css` | CSS 變數（色票、尺寸）|
| `css/navbar.css` | 上方導覽列樣式 |
| `css/sidebar.css` | 側邊欄樣式 |
| `css/components.css` | 共用元件樣式 |
| `css/responsive.css` | RWD 斷點覆寫：≤992px 側欄改浮層 (overlay) + `#sidebar-backdrop` 遮罩、隱藏麵包屑；≤768px 手機壓縮（語言/意見箱僅留圖示、App Grid 縮格）；≤480px 抽屜近全寬。對應行為（初載收合、遮罩點擊、跨斷點還原）在 `js/ui/layout.js`。|

### 4.2 js/ 根層（核心）

| 檔案 | 職責 |
| --- | --- |
| `main.js` | 進入點：載入 modals partial、初始化 Modal 實例、啟動登入流程 (`restoreLoginFromStorage` 內建雙重驗證 `_currentServerEmpId` 與 `Object.assign` 全欄位同步快取防呆)、綁定全域事件（含防重複綁定 `_mainJsEventsBound`）、監聽 `storage` 事件做多分頁同步。|
| `store.js` | 狀態中心：收斂原先掛載在 `window.*` 上的 20 多個全域變數，統一匯出為 `appState` 以供各模組 `import`。|
| `config.js` | 全域變數（已移至 `store.js`）、i18n、`escapeHTML`、`savePersonalSettings`（回傳成敗、localStorage 於 POST 成功後才寫）。|
| `api.js` | DB 讀寫層：全域 `fetch` 攔截器（自動補 `X-Requested-With` / CSRF token）、`clearAppCache` App Shell 快取防衛、欄位大小寫轉換（讀 `getVal` 無視大小寫、寫 `getDatabasePayload` 顯式 PascalCase）、各 RESTful API 封裝（`saveXxxAPI`/`batchSaveMenusAPI`/`fetchInitialDataFromDB` 並行載入且設 `cache: 'no-store'`）。|
| `auth.js` | 登入 UI 流程：Windows 自動 (`WhoAmI` 設 `cache: 'no-store'`) / LDAP 手動兩 tab、登出旗標 `umc_force_manual_login`。|

### 4.3 js/ui/（版面與互動）

| 檔案 | 職責 |
| --- | --- |
| `navigation.js` | 上方導覽列、語言切換、選單路徑、`goDefaultHome`（用過濾後清單防呆）、`navTo`/`openDynamicIframe`（iframe sandbox 依同源/跨源調整）。|
| `layout.js` | 版面控制：`toggleSidebar`/`togglePin`/`toggleFullscreen`/`switchLayoutMode`/`enforceSystemModeUI`。|
| `dialogs.js` | 全域對話框 `customAlert`/`customConfirm`（預設 escapeHtml、`isHtml` flag 才允許 HTML；錯誤/決策情境用）、`showToast`（非阻斷右上角 Toast，成功/資訊回饋專用）、`skeletonRows`（查詢表格載入骨架屏）、`generateIconHtml`、`updateSyncButtonUI`。|

### 4.4 js/render/（畫面渲染）

| 檔案 | 職責 |
| --- | --- |
| `sidebar.js` | 側邊欄渲染、權限展開（`getAllowedIdsWithHierarchy`/`getMenuPermissions`）、頭像下拉、看板即時搜尋（`filterSidebarMenus`，只讀已過濾的 `_currentValidMenus`）；DataTable 共用工具 `safeDestroyDataTable`/`initDataTable`（**含分頁記憶 `_dtPageMemory`：摧毀前記住分頁、重建後 clamp 還原，使管理表 CRUD 後留在原分頁、不跳回第一頁**）。|
| `sidebar-item.js` | 單一側邊欄項目 HTML（`generateSidebarMenuItem`）、子選單開合 `toggleSubMenu`。|
| `tables.js` | 各後台管理表格渲染（多數為 client-side DataTables，每表走 `safeDestroyDataTable`→重建 tbody→`initDataTable`，分頁還原由 sidebar.js 集中處理）；**唯 `renderAccountTable` 為 server-side DataTable**（`serverSide:true`＋自訂 `ajax` 打 `/api/Accounts?page=&pageSize=&q=`，因帳號數可成長到數萬），個人模式切換 `togglePersonalProp`。|
| `account-ui.js` | 帳號編輯 Modal 內的權限 UI（Role/委派看板/預設頁 checkbox 與計算）＋**per-fab 個別覆寫面板**（`renderAccOverridePanel`：廠區選擇器＋該廠區 額外開放/個別封鎖/即時預覽，狀態 `appState.tempExtraMenus/tempDenyMenus/overrideFab`）。|

### 4.5 js/admin/（後台管理功能）

| 檔案 | 職責 |
| --- | --- |
| `modal-utils.js` | Modal 安全開關 `showModalSafely`/`hideModalSafely`、ACL 可見性切換。|
| `fab-manage.js` | 廠區 CRUD（`openAddFabModal`/`editFab`/`saveFabItem`/`deleteFab`）。|
| `role-manage.js` | 權限群組 CRUD + 看板勾選（`saveRoleItem`/`toggleRoleMenuSelection`…）。|
| `account-manage.js` | 帳號 CRUD + 委派/預設頁 UI（`saveAccountItem`/`toggleAccDelegationUI`…）。`editAccount` 先打 `/api/Accounts/{id}` lazy-load 明細（不依賴只剩自身列的 `getAccounts()`）；工號查重改靠後端。|
| `menu-manage.js` | 看板/網頁/節點 CRUD + 樹狀拖曳建構（`saveMenuNodeItem`/`buildTreeUI`/`initTreeDragAndDrop`/`parseTreeDOM`…）。|
| `misc-manage.js` | Excel 匯出入（`exportConfig`/`importConfig`，匯入二次確認；`createWorkbookData` 為 **async**、帳號 sheet 改打 `/api/Accounts/export` 取全量）、拖曳排序（`reorderSystemMenu`/`reorderPersonalMenu`/`reorderWebpageMenu`，系統版面走 `batchSaveMenusAPI`、個人版面走 `/api/PersonalSettings`）、App/申請/稽核相關操作、圖示選擇與壓縮。|
| `activity-log.js` | 操作紀錄載入/分頁/清除（`loadActivityLogs`/`changeActivityPage`/`purgeActivityLogs`）；篩選 Enter 送出、新查詢重設第 1 頁。|
| `traffic-stats.js` | 流量統計頁（`#page-traffic-stats`，admin-only）：打 `/api/Analytics/UsageStats`/`details` 渲染 DAU/MAU KPI、趨勢圖與每日造訪明細（`escHtml` 防禦、`skeletonRows` 載入態、i18n 全量覆蓋）。|

---

## 5. 關鍵跨檔機制（速查）

- **請求流程**：瀏覽器 → `api.js`（補 CSRF/XHR 標頭）→ Controller → Service → `AppDbContext`/raw SQL → MSSQL。回應經 `GetInitialData` 快取（含 ETag/304）。
- **快取與 ETag**：`SettingsService` 雙層快取（Global 60s / Volatile 10s）；靜態 `_currentETag` 由 `InitialDataCacheInvalidator`（Singleton，集中清快取+bump）bump，同時作廢 `MenuAuthService` 的 `visibleMenus:{ETag}:{empId}` 可見集合快取。另有 `CacheInvalidationInterceptor`（EF `SaveChangesInterceptor`）作為安全網自動攔截 EF 寫入。**raw ADO.NET 寫入路徑（不走 EF tracking）仍必須手動呼叫 Invalidate**。
- **權限判定**：後端 `MenuAuthService`（優先序 Menu ACL > Account override > Role，走 parent chain）；前端 `getMenuPermissions` 對齊。非 admin 的 `GetInitialData`/`GetMenus` 一律後端列級過濾。
- **圖示儲存**：一律 base64 → 實體檔，DB 只存 `/images/icons/{guid}.{ext}`，統一走 `IIconStorageService`。
- **稽核**：Middleware → `ActivityLogQueue`（Channel）→ `ActivityLogProcessor`（批次寫入 `UserActivityLogs`）；`ActivityLogPurgeService` 依 RetentionDays 定期清舊。
- **流量統計**：登入統計（`SettingsService.UpdateLoginStats`）→ `RecordDailyUserVisitAsync` 冪等 upsert `DailyUserVisits`（DB 端 `GETDATE()` 為「今天」基準）→ `AnalyticsController`（admin）聚合查詢 → 前端 `admin/traffic-stats.js` 渲染。
- **個人版面**：per-user 走 `/api/PersonalSettings`；localStorage 只快取「登入者自己」一份且每次以 DB 為準覆寫。
- **Schema**：無 EF Migrations，啟動時 `SchemaBootstrap` idempotent 修復；索引集中 `EnsureIndexesAsync`。

---

*本文件為現況快照，結構或職責變動時請同步更新。*
