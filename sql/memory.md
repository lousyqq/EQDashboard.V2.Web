# 專案記憶庫 (memory.md)

> 現況快照（非逐日日誌）。2026-06-07 經使用者要求，將累積的「逐日決策日誌／踩坑流水帳／多輪安全審計歷史」壓縮為下列「仍生效的核心知識＋教訓」。完整歷史可追 git。

## 🏗️ 1. 系統核心與隱藏邏輯
- 系統 UI（廠區切換、權限綁定）高度依賴完整的資料表關聯（`Map_Fab_Role`、`Map_Account_Role`…），缺一不可。
- **前端為 ES Modules**：`index.html` 唯一進入點 `<script type="module" src="js/main.js">`，由 main.js `import` 整張模組圖。**所有 import 必須置頂**（任一模組 SyntaxError 會中止整張圖，畫面卡「載入中…」、登入/登出全失效）；模組內 `function X` 需 `window.X = X` 才能給 HTML inline `onclick`。
- `partials/modals.html` 由 `fetch()` 非同步載入插入 `#modals-container`，在 JS 初始化前完成。
- 後端 Service 層 + DI（`ISettingsService`/`IAccountService`/`IAuthService`/`IMenuAuthService`），Controller 只負責路由與回傳。`AppDbContext` 用 `ApplyConfigurationsFromAssembly`（每 Entity 一個 `Data/Configurations/*Configuration`）。`AuthService` 用 `IOptionsSnapshot<AuthSettings>`。
- **無 EF Migrations**：schema/索引由 `SchemaBootstrap`（啟動時 idempotent raw SQL）自我修復；EF `HasIndex` 對既有 DB 不會真的建索引。索引單一事實來源＝`SchemaBootstrap.EnsureIndexesAsync`。
- `GetInitialData` 回應有 10s `IMemoryCache` 快取；任何寫入端點都必須 `InvalidateInitialDataCache()` 同步，否則前端取回舊快照（看似沒存到）。快取僅在全部表載入成功才寫入。
- 操作紀錄：`ActivityLoggingMiddleware` → `ActivityLogQueue`(Singleton `Channel<>`，`FullMode=Wait`＋`TryWrite` 非阻塞，滿載**記 Warning 告警不丟最舊**) → `ActivityLogProcessor`(BackgroundService，`DequeueAsync` 等第一筆＋`TryDequeue` drain 成批、`MaxBatchSize=100`、`AddRange`＋**單次 SaveChanges**、批次失敗退回逐筆隔離毒資料、每工作單位 `CreateScope()` 解析 Scoped DbContext)。已跳過成功 GET 以降低寫放大。
- **圖示一律存實體檔、DB 只存路徑 `/images/icons/{guid}.{ext}`**：統一走 `IIconStorageService`（Scoped）。`SaveAsync` data: URI→白名單寫檔、絕對自我參照 URL→正規化相對、FA class/外部 URL→原樣、非白名單 data:→丟棄；`DeleteIfLocalUnreferencedAsync` 於 update/delete commit 後做參照檢查＋path-traversal 防護的孤兒清理；`MigrateBase64IconsAsync` 啟動時一次性轉舊 base64。欄位 `App.IconBase64` 名稱不變但內容已是路徑。Controller **不可**自行存 base64 或拼路徑。
- **`MenuAuthService` request-scoped 快取**：因 DI 為 Scoped＝每 HTTP 請求一實例，故 `_childToParents`/`_canEditOthersCache`/`_manageSetCache` 在同請求內快取安全；安全前提＝所有權限檢查都發生在結構異動「之前」，不會讀到同請求剛寫的髒資料。改動 MenusController 寫入順序時須維持此前提。

## 🐛 2. 踩坑與填坑紀錄（仍生效的教訓）
- **取 EmpId 一律走 `ClaimTypes.NameIdentifier`，禁用 `User.Identity.Name`**（後者是姓名，會讓 `Map_Account_ManageMenu` 委派判定全找不到、委派 user 寫入全 403）。
- **Class-level `[Authorize]` 一律設最寬鬆 baseline**；要 admin 的 action 自己加 `[Authorize(Roles="admin")]`。class+action 的 `[Authorize]` 是「累加」不是 override —— class 設 admin 會把所有非 admin 擋死、整站無 sidebar/廠區/menu。
- **寫入端點 path id 為事實來源**（函式開頭 `dto.Id = id;`／`dto.EmpId = empId;`）。否則 path 拿來做權限檢查、body 的 Id/EmpId 拿來寫 mappings → 攻擊者可洗掉他人 mapping、替機密目標掛上未經檢查的 mapping/ACL。
- **`createdBy` 強制為實際登入者**（更新時 immutable），防委派 user 偽造成 admin 建立、混淆 `isMyOwn` 判定。
- **非 admin 編輯 menu 一律清空 `dto.AllowedEmpIds/DeniedEmpIds`**（那是「決定他人看不看得到」的工具，委派 user 不可動，否則能把正常 user 踢出看板）。
- **`MenuAuthService` 權限要走 `Map_Menu_Structure` parent chain**（對齊前端：folder 委派＝整個子樹可編）：admin → 自己 createdBy → direct delegation → ancestor delegation + CanEditOthers → 否則 deny。優先序 Menu ACL > Account override > Role。
- **非 admin 的 `GetInitialData`/`GetMenus` 必須後端按可見性過濾**（`IMenuAuthService.GetVisibleMenuIdsAsync` + `SettingsController.FilterTable`），不可只靠前端 UI 篩。個人表（Accounts/Requests/PersonalSettings/Map_Account_*）只留自己。
- **`SaveDataAsync` 寫入用批次參數化 INSERT，禁止改回 `SqlBulkCopy`**：Sariel 僅 6GB RAM，bulk load 需 workspace memory grant，在記憶體壓力下排隊等 `RESOURCE_SEMAPHORE`（曾達 196s）；INSERT…VALUES 不需 grant、免疫。
- **拆分 JS 必須在函式邊界切割並逐檔 `node --check`**：曾因切點橫跨函式結尾 `};`、或在非 async 函式內加 `await`，導致整檔 SyntaxError 無法載入、按鈕無反應。
- **Excel 匯入格式判斷**：不可只依賴 `Accounts` 頁籤的 `EmpId`（admin 不在表內時空陣列會被誤判 V1 舊格式、清空全表）；須同時判 `Menus`/`Roles` 的 V2 專有欄位。
- **EF 模型須對齊 DB schema**（NVARCHAR PK，非 INT PK），否則寫入全失敗。
- **`SchemaBootstrap` 的 `LogError` 需包 try-catch**（缺 EventLog 權限的環境會連記 log 都拋例外、崩潰啟動）；整個 RunAsync 失敗只記 log、不擋啟動。
- **iframe sandbox**：cross-origin URL 拿掉 `allow-same-origin`（防外部頁用 `parent.document` 操作本站 DOM）；same-origin 維持原配置。
- **DTO 驗證**：RoleLevel 限 `admin|user`；Request Status 對齊前端 `pending|processing|resolved|rejected|withdrawn`；圖示/URL base64 設大小上限（避免撐肥 cache）；list 元素層級驗證在 service 內 silent-drop。
- **EF 批次 upsert 大小寫陷阱**：`Map_Menu_Structure`/Menus PK 為 NVARCHAR，SQL 比對不分大小寫但 EF 的 `ToDictionary` 預設 ordinal（分大小寫）。`BatchUpdateMenus` 的 `existingMap` 必須用 `StringComparer.OrdinalIgnoreCase`，否則同一 PK 大小寫不同會被當新列 `Add` → INSERT 撞主鍵爆掉。
- **EF 同批 RemoveRange + Add 同一 PK 要分兩次 flush**：先 `RemoveRange`＋`SaveChanges` 真正刪掉，再 upsert＋`SaveChanges`，否則同一交易內刪舊加新會撞追蹤衝突。`BatchUpdateMenus`/`BatchDeleteMenus` 採此模式。
- **圖示渲染判斷前後端要一致**：前端 `iconVal.startsWith('data:') || iconVal.includes('/')` → `<img>`；否則 FA `<i class>`（FA class 永不含 `/`）。散落 `render/sidebar-item.js`/`render/sidebar.js`/`ui/dialogs.js`/`admin/misc-manage.js` 四處，改一處要同步。（misc-manage.js 另一處 `startsWith('data:image')` 是 Excel 匯出長度防呆，非渲染判斷。）
- **圖示路徑 passthrough 別只認相對路徑**：舊 `SaveIconAsync` 只 passthrough 相對 `/images/icons/`，App 更新存的是絕對 URL（`http://host/images/icons/...`）→ 被當新檔。`IconStorageService.SaveAsync` 兩者都正規化成相對才存。

## 🛤️ 3. 開發歷史與決策日誌
> 舊歷史已壓縮（見檔首說明）。僅保留最近里程碑：
- 2026-06-07: **架構複查（唯讀，未改碼）**。全面複讀 13 controllers + 4 services + middleware/queue/processor。安全面健康（IDOR/mass-assignment/server-side 過濾/rate-limit/CSRF/health 硬化皆到位）。列出待修清單見 §4。
- 2026-06-07: **架構複查收尾 + 文件壓縮**。本日完成：(A) stale-module 快取 `.js/.css` 改 no-cache；(C) Swagger 限 Development；升 Swashbuckle 7.2.0；快取殘缺資料修復、`ClientIpHelper` 抽共用、CSRF 失敗回 JSON；(a) 側邊欄看板即時搜尋框（跨 `_currentValidMenus`、絕不退回未過濾清單）；(e) 啟用 ResponseCompression（Brotli+Gzip）瘦身 GetInitialData；(甲) 索引 schema drift 根治 —— `SchemaBootstrap.EnsureIndexesAsync` 集中建 5 個效能索引、移除 3 處 EF `HasIndex` no-op、刪 inline CREATE INDEX，實機驗證 `sys.indexes` 5 索引全在；(乙) `/health` 硬化 —— liveness（不碰 DB、公開）/ `/health/ready`（含 DB、限 loopback+私有網段、否則 404、IP 走 `RemoteIpAddress` 非 XFF），實測通過。最後依使用者要求把 CLAUDE.md/memory.md 壓成現況快照。
- 2026-06-07: **修復 DOM XSS 漏洞**。重新檢測時發現 `ui/dialogs.js` 中的 `customAlert` 以及 `main.js` 中的 `loadingOverlay` 錯誤訊息未經跳脫即透過 `.innerHTML` 渲染，存在 DOM-based XSS 風險。已將其修改為預設透過 `window.escapeHtml` 跳脫處理，並針對允許 HTML 的情境加入 `isHtml` flag，確保任何不可信的來源（如 API 錯誤訊息）都不會遭到惡意腳本執行。
- **2026-06-07**: 執行前端嚴謹模組化。為遵守專案不引入打包工具 (Vite) 的規範，透過 Node.js 腳本將原本掛載在 `window.*` 上的 20 個全域狀態變數 (如 `currentFab`, `currentUser`)，統一收斂至獨立的 `store.js` ES Module 並匯出為 `appState`。同時將 17 個 `.js` 檔案全面重構，改為 `import { appState } from './store.js'`，徹底解決了全域變數污染問題，且並未破壞任何現有之內聯 (inline) 事件綁定。
- **2026-06-07**: 確認後端 Super Admin API 防護已生效。先前之 `AccountService.cs` 已包含嚴謹之邏輯，成功阻擋對 `empId="admin"` 的降級與刪除操作。
- **2026-06-07**: 緊急修復前端 ESM 重構後造成的 `modals is not defined` 與 `systemAlertModalObj is not defined` 錯誤。原因為 `main.js` 在初始化 Modal 實例時未加上 `appState.` 前綴，已全面盤點並修正所有遺漏的隔離狀態變數。
- **2026-06-07**: 緊急修復 `Uncaught SyntaxError: The requested module '../main.js' does not provide an export named 'initDashboardUI'` 與 `appState.appState` 雙重前綴 Typo。原因為前次修復時工具匹配失誤導致 `main.js` 頂部模組宣告遺失，且腳本誤將變數名稱雙重替換。已透過嚴謹腳本重構並還原 `export` 關鍵字與變數名稱，徹底解決 ESM 導入與 undefined 崩潰問題。
- **2026-06-07**: 修復 `admin` 帳號的上方導覽列顯示過多非指派看板的問題。原架構中 `admin` 會強迫繞過權限群組交集，直接顯示該廠區下「所有權限群組」的聯集看板。已依照使用者要求移除此繞過機制 (`sidebar.js`, `navigation.js`, `tables.js`)，現在 `admin` 亦將嚴格遵循其自身被指派的權限群組 (`assignedRoles`) 來呈現看板組合，實現所見即所得。
- **2026-06-07**: 釐清並修復「上方導覽列出現 1111，且缺少 ZE」的不一致問題。經追查為資料異常：使用者先前於「看板網頁管理」中建立了名為 `1111` 的測試資料夾，並誤將原本的 `ZE` 節點拖曳為 `1111` 的子節點。由於 `ZE` 仍被「12A 完整模組」權限允許，系統基於樹狀權限繼承邏輯，自動顯示其父節點 `1111` 於上方導覽列。已直接從資料庫清理無效的 `1111` 節點並將 `ZE` 恢復為根節點，使導覽列與權限群組標籤完美對齊。
- **2026-06-07**: 二次修復「登入次數重新整理變為 1 次」的問題。先前修復 `main.js` ESM 模組遺失的 `git checkout` 操作意外覆蓋了 `loginCount` 的同步更新邏輯。已撰寫高安全性替換腳本重新補回 `main.js` 的 `tempUser.loginCount` 與 `tempUser.lastLoginTime` 同步邏輯，並更新了 `<script>` 的版本號以強制清除瀏覽器快取。
- **2026-06-07**: 釐清「新增主選單 111 會自動出現在導覽列」的機制。使用者在新增 `111` 後，將原有的 `N-Sys` 看板拖曳進了 `111` 之中。由於 `N-Sys` 本身是被授權的看板，系統必須讓使用者有路徑可以點擊到 `N-Sys`，因此會自動將其父層資料夾 `111` 顯示在上方導覽列。這是正常的「樹狀權限繼承」行為。
- **2026-06-07**: 調整上方導覽列 `rootMenus` 呈現邏輯以符合使用者「嚴格依照群組設定」的需求。原 V2 架構中存在一個「若子節點允許則強制允許父節點」的迴圈，導致未被明確指派至廠區群組的父資料夾（如 `111`）會因內含被授權的子看板而自動浮現於上方導覽列。已從 `sidebar.js` 移除此自動補齊父層的迴圈，現在只有明確被設定在廠區群組內的選單才會出現在上方導覽列，未被指派的父資料夾將強制隱藏，而其被授權的子看板則會自動提升為根節點顯示。
- **2026-06-07**: 徹底解決 `modals is not defined` 與 `does not provide an export named 'initDashboardUI'` 問題。前次因緊急還原而覆蓋了 `main.js` 的 ESM 重構代碼，且因各個 `.js` 檔案內部的 `import ... from '../main.js'` 未加上版號，導致瀏覽器嚴重快取到舊版壞掉的模組。已撰寫腳本遞迴將所有 `.js` 內部互相引入 (import) 的路徑全數押上強勢快取清除碼 `?v=20260607h`，並重新將 `main.js` 中的 `modals` 修正為 `appState.modals` 且還原 `export` 關鍵字。同時修復了轉換腳本意外造成 `appState.appState.systemAlertModalObj` 雙重前綴導致的 Cannot set properties of undefined 錯誤，確保前端生命週期正常運作。
- **2026-06-07**: 修復 V2.Web 左側子選單 (如 WL子群組) 點擊無法展開的問題。原本試圖自製 `toggleSubMenu` 處理展開收合，但與 DOM 狀態不匹配導致點擊失效；現已還原使用 Bootstrap 原生的 `data-bs-toggle="collapse"` 並移除 `.collapse` 上的 `display:none` inline style。同時強制更新了 ES Module 的快取版本碼至 `20260607c`。
- **2026-06-07**: 修復頁面重新載入 (F5) 時，登入次數與最後登入時間被還原為 1 與預設值的問題。原因為 `restoreLoginFromStorage` 漏了將從資料庫拿回的最新 `loginCount` 與 `lastLoginTime` 更新到 `currentUser` 中。
- **2026-06-07**: 根據使用者反饋，將「權限群組管理」表格中的「允許的看板組合」文字修改為「允許的選單組合」，以避免使用者誤解其包含範圍，並更新多國語系設定檔 `config.js` 與 `index.html`，同步推進前端快取版號至 `20260607i`。
- **2026-06-07**: 修復重新整理 (Ctrl+F5) 後右上角選單內的「本次登入時間」顯示為 `00:00 AM` 的問題。原因為重新整理時只會取得資料庫回傳的 `lastLoginTime`，而前端遺失了 `currentLoginTime` 的暫存。已修改 `sidebar-item.js` 中的時間渲染邏輯，當缺乏暫存時自動將 `lastLoginTime` 格式化並顯示，並將系統版號推進至 `20260607j` 確保更新。
- **2026-06-07**: 根據使用者需求，將右上角選單內的「本次登入時間」顯示格式由原先的 12 小時制 (例如 `11:15 PM`) 修改為精確的 24 小時制完整格式 `YYYY/MM/DD HH:mm:ss` (例如 `2026/06/07 23:05:45`)。已同步修改 `auth.js` 初始登入時間邏輯與 `sidebar-item.js` 的重整還原邏輯，確保各種情境下的時間顯示格式皆保持一致，並推進系統版號至 `20260607k`。
- **2026-06-07**: 優化右上角使用者選單的 UI 版面配置，以容納較長的精確時間字串。將選單寬度由 280px 稍微加寬至 310px，並為「本次登入時間」套用 `text-nowrap` (強制不斷行) 與 `font-size: 0.85rem`，解決時間過長導致排版跑版的問題。
- **2026-06-07**: 修復上方導覽列「系統/自訂」版面切換按鈕點擊無效的問題。由於前次 ES Modules 架構重構時，全域變數 `currentLayoutMode` 已被封裝至 `appState.currentLayoutMode`，導致 `index.html` 內聯寫入的 `onclick` 事件因為找不到變數而拋出 `ReferenceError` 錯誤中斷。已將該 inline handler 修改為正確存取 `(window.appState && window.appState.currentLayoutMode)`。
- **2026-06-07**: 修復全域確認對話框 (`systemConfirmModal`) 的「確定」按鈕點擊無效的問題。與前述變數被封裝的原因相同，內聯寫入的 `onclick` 事件無法找到已被收攏至 `appState.confirmActionCallback` 的全域變數。已將 `index.html` 內的確認按鈕點擊事件修改為正確呼叫 `window.appState.confirmActionCallback()`。
- **2026-06-07**: 修復點擊左側「資料庫與同步」(設定檔管理) 選單時，畫面呈現一片空白的問題。問題根源為 `index.html` 內的 `page-activity-log` 區塊漏掉了一個 `</div>` 標籤，導致後續的所有頁面區塊（包含 `page-config-manage` 等）全部變成它的子元素。當切換到設定檔頁面時，父元素 `page-activity-log` 被隱藏，連帶導致底下所有的子元素都無法顯示。已將遺漏的 `</div>` 補齊，恢復正確的 DOM 結構。
- **2026-06-07**: 執行 ES Modules 架構升級後的全面程式碼盤點。利用腳本掃描所有 `index.html` 與 `modals.html` 內的 `onclick`、`onchange`、`onsubmit` 內聯事件綁定，並交叉比對 `window.*` 全域函式暴露清單。確認所有近 50 個前端綁定事件（如 `saveFabItem`, `exportConfig`, `doLogin` 等）皆已正確導向模組內的公開函式，且動態產生的 HTML 模板（如 `togglePersonalProp`）亦皆綁定無誤。所有全域變數（如 `appState`）的存取皆已修復為 `window.appState`。確認無遺漏的架構災情。
- **2026-06-07**: 修復「個人頁面管理」還原預設版面時的排序基準不一致問題。在系統版面中，上方導覽列（根節點）是依照角色所配置的 `AllowedMenuIds` 順序進行渲染，但先前在個人頁面的回退邏輯中，若無個人配置的 `order` 屬性，則會一律回退到全域的 `GlobalOrder`，導致還原後與系統版面的呈現順序不一致。現已將 `renderPersonalMenuManage` 內的回退機制修改為：根節點比照系統版面採用 `dedupedInitIds` 陣列順序，子節點則優先對齊掛載時的 `parentOrders` 順序，確保還原預設配置的行為與預期 100% 吻合。
- **2026-06-07**: 修復右上角使用者下拉選單（包含「累積登入次數」、「本次登入時間」、「登出系統」等字樣）以及登入來源徽章（如「測試」）在切換英、日語系時，沒有正確被翻譯的問題。原因為模組化升級後，`config.js` 內的 `i18n` 變數變成了模組區域變數，導致 `navigation.js` 在執行 `changeLanguage` 時判定 `typeof i18n` 為 `undefined` 而跳過了所有帶有 `data-i18n` 的 HTML 標籤翻譯。現已將 `i18n` 匯出並掛載至 `window.i18n`，並補齊了登入來源徽章的翻譯字典。
- **2026-06-07**: 盤點當前 C# EF Models (`Account.cs`, `Menu.cs`, `UserActivityLog.cs` 等 18 張實體與關聯表) 以及 `SchemaBootstrap.cs` 的動態結構更新腳本，確認與 `DB_Table.md` 中的全量 `CREATE TABLE` 腳本與索引定義 100% 吻合，資料庫架構並無新增或變更。
- **2026-06-07**: 釐清「上方導覽列全部顯示」的問題。系統目前會根據切換到的廠區，顯示該廠區「所綁定的所有權限群組」的交集選單。這是資料設定的正常行為（如 12A 廠區在後台被勾選了 3 個群組，切換到 12A 時就會顯示 3 個群組的模組）。已請使用者調整廠區管理的設定。
- **2026-06-07**: 修復 `main.js` 缺失 `restoreLoginFromStorage` 與 `initModalInstances` 匯出定義，導致 `auth.js` 載入失敗引發的無法登入問題。已補上 `export` 關鍵字並推進系統版號至 `20260607d`。
- **2026-06-07**: 修復 `main.js` 因殘留無效的具名匯入 (Named Imports) 導致瀏覽器拋出 `SyntaxError` 的問題。由於系統所有的 ES 模組皆已將函式綁定至 `window` 全域物件，現已將 `main.js` 頂部的依賴改為純執行匯入 (Bare Imports)，徹底解決匯入名稱不匹配的問題。
- **2026-06-07**: 應使用者要求移除預設的「歡迎登入」頁面。已將 `index.html` 中的 `#page-home` 改為「無權限瀏覽」的提示訊息，並修改 `main.js` 登入初始化流程：登入後自動執行 `goDefaultHome()`，跳轉至使用者有權限的第一個看板；若完全無看板權限，則自然停留在「無權限瀏覽」畫面。
- **2026-06-07**: 修復群組編輯中「拖曳允許存取的看板」排序後，上方導覽列順序未跟著改變的 Bug。原因為 `sidebar.js` 在渲染選單時，將「針對 root 節點的群組排序」與「針對子節點的 order 排序」混用在同一個 `Array.prototype.sort()` 比較函式中，導致非遞移性比較 (Non-transitive comparator) 崩潰，瀏覽器排序結果隨機化。現已將 rootMenus 獨立拉出進行嚴格排序。
- **2026-06-07**: 修復群組編輯中「允許存取的看板」完全無法拖曳的問題。原因為重構時 `rmDragStart` 漏了 HTML5 拖曳必備的 `e.dataTransfer.setData('text/plain', id)` 宣告（否則現代瀏覽器會直接中止拖曳），並且 `misc-manage.js` 中殘留了重複定義的拖曳函式導致相互覆蓋。已補回 `setData` 並清除無效的重複函式。
- **2026-06-07**: 修復按 F5 重新整理後，畫面上的「登入次數」會固定變成 1 次、且登入時間變成 00:00 的問題。原因為 `main.js` 在執行 `restoreLoginFromStorage()` 藉由 `localStorage` 快速還原登入狀態時，雖然有去資料庫撈回最新的權限設定，卻漏了把 `LoginCount` 與 `LastLoginTime` 補回給當前的 `currentUser` 物件，導致 UI 渲染時因值為 `undefined` 而觸發 `|| 1` 的預設值機制。現已將登入次數與時間正確補回。
  - **圖示 base64 → 實體檔（D1/D2）**：新增 `IIconStorageService`/`IconStorageService`（DI Scoped）。`SaveAsync` 把 data: URI 依 **MIME 白名單**寫檔回傳 `/images/icons/{guid}.{ext}`、絕對自我參照 URL 正規化成相對、FA class/外部 URL 原樣保留、非白名單 data: 丟棄("")。`DeleteIfLocalUnreferencedAsync` 在 update/delete commit 後做「參照檢查＋`Path.GetFileName` path-traversal 防護」的孤兒清理。`MigrateBase64IconsAsync` 啟動時一次性把 DB 既有 base64 轉檔。`AppsController`/`MenusController` 全改走它（刪掉舊 `SaveIconAsync`）。Menu.Icon 與 App.IconBase64（欄名不變、內容變路徑）統一策略。
  - **順手修潛在 bug**：舊 `SaveIconAsync` 在 App 更新時存「絕對 URL」（passthrough 只認相對 `/images/icons/`）→ 新 `SaveAsync` 兩者都正規化成相對。
  - **D3**：`GetInitialData` 例外時先設 `Response.StatusCode = 500` 再回 `{error:true}`（前端已處理 `!response.ok`）。
  - **O1**：`BatchUpdateMenus` 一次預載受影響 menus（`Where(Contains)`＋Includes）、`existingMap` 用 **OrdinalIgnoreCase** 字典（對齊 SQL 不分大小寫，避免重複 PK insert）、RemoveRange 後**單次 flush**、再 upsert＋單次 SaveChanges＋Commit，最後統一孤兒清理；`BatchDeleteMenus` 同模式收集 oldIcons 後清理。
  - **O3**：`GetNodePermissionsAsync` 刪掉無人使用的 `isAncestor` BFS 死碼，改回 4-tuple；`MenuAuthService` 加 request-scoped 快取（`_childToParents`/`_canEditOthersCache`/`_manageSetCache`，Service 為 Scoped＝每請求一實例），所有權限檢查都在結構異動「之前」故不會讀到同請求髒資料。
  - **O4**：`UpdateLoginStatsAsync` 移除每次登入的 `IF COL_LENGTH ALTER` 探測（欄位已由 `SchemaBootstrap.EnsureAccountStatsColumnsAsync` 啟動時保證）。
  - **A1**：採「文件對齊」—— CLAUDE.md §6.2 明記 Service 層為「部分抽離」（Settings/Accounts/Auth/MenuAuth/Icon 抽 Service，其餘單純 CRUD 留 Controller，刻意不過度抽象），不硬抽 6 個 controller。
  - **延後 O2/A2**（已向使用者說明）：O2 雙重序列化只在 10s 快取重建跑一次、~111 列、重構會牽動 `FilterTable` 契約＝高風險低報酬；A2（時間戳 Unix-ms vs DateTime）純美觀、要動 schema+資料+前端遷移、零功能收益。
  - **驗證**：`dotnet build` 0 警告 0 錯誤；`node --check` 四個前端模組語法 OK；實機跑啟動 log「✅ IconStorage 一次性遷移：1 筆 base64 icon 已轉為實體檔」、第二次啟動 0 轉換（idempotent）、磁碟確有檔、`/health` 200。
- 2026-06-07: **全功能健康檢查（唯讀，未改碼，使用者要求出報告）**。靜態追讀前端寫入路徑＋後端對應端點。核心發現：
  - **(H1 最重要) 看板拖曳排序仍走全量覆寫**：`reorderSystemMenu`／`reorderWebpageMenu`（misc-manage.js）異動後呼叫 `syncDataToDB()` → `/Settings/SaveData`（admin-lock）DELETE+INSERT **全 17 表**，且 payload 的 `PersonalSettings` 由 admin 自己的 localStorage 重建（api.js 520-535）。後果：(a) 併發 lost update —— admin 拖一下排序，會用自己登入當下的舊快照覆寫整張 PersonalSettings ＋ 蓋掉其他 admin 剛改的資料 → 真實資料遺失；(b) 超重 DB 負載。**修法（低風險）**：改用既有 `/api/Menus/batch`（`batchSaveMenusAPI`，已支援 GlobalOrder＋ParentOrders→MapMenuStructure SortOrder），只送受影響的兄弟節點，完全不碰 PersonalSettings。
  - **(H2) PersonalSettings 儲存是 fire-and-forget**：`config.js savePersonalSettings` 先寫 localStorage 再 POST `/api/PersonalSettings`，POST 失敗只 `console.error` 不 throw → `commitPersonalPendingOrder` 仍顯示「已儲存成功」並清 pending；下次登入 DB 覆蓋 localStorage → 使用者排序靜默遺失。修法：`savePersonalSettings` 回傳成功與否、失敗 throw，呼叫端據此提示並保留 pending。
  - **(H3 latent) `PersonalSettingDto.Icon [StringLength(50)]`** 但 icon 路徑 `/images/icons/{32guid}.webp`=51 → 個人自訂 webp/jpeg icon 會驗證失敗被靜默 skip。修法：放寬至 200 或對齊 Menu.Icon。
  - **(H4) base64 仍可經 Excel 匯入回流 DB**：`SaveData` 大量路徑直接寫 Icon/IconBase64 欄，未經 `IconStorageService.SaveAsync`；故 `MigrateBase64IconsAsync`（啟動轉檔）即使重建 DB 後仍是有用安全網（idempotent、0 列即 no-op）。→ 回答使用者「重建後 Migrate 是否還需要」：**建議保留**，或改成 SaveData 也走 SaveAsync。
  - 其餘確認 OK：Fab/Role/Account/App/Menu(CRUD)/Requests 皆走 RESTful＋InvalidateCache；enable/disable toggle 走 saveMenuAPI；個人頁拖曳走 pending→PersonalSettings（正確）；唯一 localStorage 業務資料＝personal menus（有同步，只是 H2 缺陷）；XSS 用 escapeHTML；SaveData 已 admin-lock＋LOCK_TIMEOUT 20s＋RCSI。
  - 待使用者指示動工順序（建議 H1 > H2 > H3）。
- 2026-06-07: **修復 H1 / H2 / H3（健康檢查三項缺陷）**。使用者重申語意：系統版面＝全域共用，只有 admin 在廠區設定看板組合、一改全員一致，唯一例外是使用者的允許/拒絕黑白名單（Menu ACL）；自訂版面＝各自的上方導覽列，拖曳儲存即時更新、不可影響他人。
  - **H1（拖曳全量覆寫→batch）**：`reorderSystemMenu`／`reorderWebpageMenu`（misc-manage.js）改為 `async`，不再呼叫 `syncDataToDB()`（會用 admin 過時 localStorage 洗掉整張 PersonalSettings）。改成只把「異動的看板物件」送 `batchSaveMenusAPI()`（`/api/Menus/batch`）。`reorderSystemMenu` 收集 siblings 的 realMenu 進 `affected`；`reorderWebpageMenu` 先用 `oldOrderMap` 快照舊 order、reassign 後 filter 出真正變動的才送。樂觀渲染先呈現，`!result.success` 時 `customAlert`＋`fetchInitialDataFromDB()` 回滾重畫。BatchUpdateMenus 會從 dto 重建 SortOrder 與（admin）ACL，故黑白名單完整保留；委派管理員在自身子樹的排序也因此能存（舊 SaveData 路徑會 admin-lock 403）。
  - **H2（fire-and-forget→回傳成敗）**：`config.js savePersonalSettings` 改為 `!response.ok`/catch 回 `false`、成功回 `true`（不再只 console.error）。呼叫端：`commitPersonalPendingOrder`（misc-manage.js）改檢查回傳值，失敗保留 pending＋提示、不假報成功；`togglePersonalProp`／`setPersonalTarget`（tables.js）改 `async` await，失敗 `customAlert`＋`fetchInitialDataFromDB()` 還原重畫。（CSRF 的 `X-Requested-With` 標頭由 api.js 全域 fetch 攔截器自動補，故個人設定 POST 一直有過 CSRF，不需手動加。）
  - **H3（DTO 長度）**：`PersonalSettingDto.Icon` `[StringLength(50)]`→`[StringLength(200)]`（DB 欄位本是 nvarchar(max)，只有 DTO 擋住 51 字的 `/images/icons/{guid}.{ext}`）。
  - **H4 不動**：`MigrateBase64IconsAsync` 維持保留（Excel 匯入/SaveData 大量路徑仍可讓 base64 回流 DB，啟動轉檔是安全網、idempotent）。
  - **驗證**：`dotnet build` 0 警告 0 錯誤；`node --check` 三個改動模組（misc-manage/config/tables）語法 OK。
- 2026-06-07: **健康檢查「可優化」項全做（使用者：「可優化的部分，也幫我全部修改」）**。承接 H1/H2/H3 後的收斂工程，把 `PersonalSettings` 從全量覆寫路徑徹底剝離、登入只快取自己：
  - **O2a（後端）**：`SettingsService.TableNames` 移除 `"PersonalSettings"`。`SaveDataAsync` 依此清單 DELETE→INSERT，移除後 `SaveData`／Excel 匯入不再碰個人版面表。讀取端 `GetInitialDataAsync` 是直接 `_dbContext.PersonalSettings` 取，與此清單無關，故安全。
  - **O2b（前端）**：`api.js getDatabasePayload()` 移除「用全帳號 localStorage 重建 `payload.PersonalSettings`」整段（舊 519-535）。即使殘留也因 O2a 不會被寫入，但一併移除避免誤導。
  - **O3（前端）**：`fetchInitialDataFromDB` 不再把 `psByEmp` 內**所有人**版面寫進 localStorage；改成取 `MyProfile.empId`（fallback `window.currentUser?.id`）後**只**寫「登入者自己」一份，且每次都覆寫成 DB 真實值（含 DB 空→本機清空）。寫入點移到 MyProfile fetch 之後（`let myEmpId=''` 宣告於 try 前）。
  - **O-extra**：`importConfig` 破壞性全量覆寫前加 `customConfirm` 二次確認（確認後才跑 `runImportConfig`）；移除 `saveAppItem`／`deleteAppItem` 中 `else if (typeof syncDataToDB==='function') syncDataToDB()` 死碼後備（`saveAppAPI`/`deleteAppAPI` 為靜態 import 必為 function，分支永不觸發）。`syncDataToDB` 仍保留於 Excel 匯入（`processAndSaveWorkbook`）＝唯一合法使用者。
  - **延後（item 4）**：PersonalSettings 差分/MERGE 更新 **刻意不做**。現行 `/api/PersonalSettings` 是 per-user「先刪該 user 再插」，已 scoped、安全、量小（單一使用者數十列）；改 MERGE 反增複雜度與 bug 面，屬「低優先/量大時才考慮」的過早優化。量真的成長再說。
  - **驗證**：`dotnet build` 0 警告 0 錯誤；`node --check` 改動模組（api.js／misc-manage.js）語法 OK。
- 2026-06-07: **多 persona 登入模擬複檢（使用者：「重新模擬不同權限 Windows 帳號登入後操作各功能、檢查需修正處」）**。全面複讀 10 controllers + MenuAuthService + SettingsService.GetInitialData/FilterTable + 前端 reorder/personal/export 流程，逐 persona（未註冊 / 一般 user / 委派 admin / admin）走查。
  - **後端授權結論：全數守住**。Accounts/Roles/Fabs class-level `[Authorize(Roles=admin)]`；Menus/Apps `[Authorize]`+`MenuAuthService` 逐節點檢查（admin/owner/委派+CanEditOthers）；Requests `[Authorize]`+IDOR（只動自己）、Audit admin-only；PersonalSettings 強制 claim EmpId+visibleSet 過濾；Settings.GetInitialData 非 admin 列級過濾、SaveData admin-only、UpdateLoginStats EmpId 取自 claim；WhoAmI 擋未註冊（不發 cookie）。前端 reorder（系統版面）走 batch 逐 menu authz＋rollback、personal 走 per-user。無新增授權漏洞。
  - **發現並修正 1 項一致性缺陷（O2/O3 的尾巴）**：Excel 匯出 `createWorkbookData` 仍對「全帳號」產 `PersonalSettings` sheet，但 (a) O3 後 localStorage 只剩 admin 自己一份 → sheet 殘缺；(b) 匯入端 `processAndSaveWorkbook` 從不讀此 sheet、且 `getDatabasePayload` 已不組裝 PersonalSettings → 此 sheet 為「匯出但永不還原」的死資料（其實改版前匯入也是從 localStorage 重建、不讀 Excel sheet，本來就不 round-trip）。**修法**：移除匯出的 PersonalSettings sheet，讓「匯出＝可還原備份」一致。個人版面備份/還原一律走 DB。
  - **驗證 O2a 無 FK 回歸**：`PersonalSettings` schema 為 PK-only（複合 EmpId+MenuId）、無 FK 到 Menus，EF config 也無關聯 → 移出 TableNames 後 `SaveData` 的 `DELETE FROM Menus` 不會被 PersonalSettings rows 卡住；個人版面改為「在 Excel 全量覆寫中存活」（預期行為）。Excel 刪掉某 menu 時其 PersonalSettings 列會變孤兒，但渲染時查無對應 menu 即略過，非 crash，可接受。
  - **驗證**：`node --check` misc-manage.js 語法 OK（本輪僅動前端匯出）。
- 2026-06-07: **完整健康檢查報告（唯讀，使用者要求出兩段式報告、先不要動手）**。複讀 render/sidebar.js、render/tables.js、MenuAuthService、SettingsService、PersonalSettingsController、ActivityLog{Queue,Processor}、AccountService.Update/Delete、misc-manage 拖曳流程。結論：**無高危漏洞**（後端授權/IDOR/server-side 過濾全守住；RESTful 帳號更新以 tracked entity 部分更新＝`LoginCount`/`LastLoginTime` 不會被洗 null，null-wipe 只存在於 legacy `SaveData` Excel 路徑；刪帳號會連帶清 PersonalSettings＋五張 Map 表＋soft-delete backup）。提出之**待修/可優化清單（尚未動工）**見 §4。重點發現：(A1) `SaveData` 的 `ALTER TABLE NOCHECK CONSTRAINT ALL` 是跨 session 的持久 metadata、`WITH NOCHECK CHECK` 重啟不重驗→FK 變 untrusted；交易期間以鎖序列化他人寫入（LOCK_TIMEOUT 20s 可能讓他人儲存失敗）。(B1) `ActivityLogQueue` `BoundedChannel(1000, DropOldest)` 高負載靜默丟最舊稽核。(E4) `ActivityLogProcessor` 逐筆 SaveChanges＝寫放大。(E1) 每次 RESTful 寫入整包 `InvalidateInitialDataCache` → 連續編輯時快取頻繁全失效重載 17 表。(F1) `tables.js` 底部 `window.X=X` 匯出區塊重複三遍＝純冗餘。其餘：E2 非 admin 載全表再記憶體過濾、E3 ConvertToList 雙序列化、E5 PersonalSettings 每次整人 delete+insert、E6 GetVisibleMenuIdsAsync 撈整張 Map_Menu_AllowAccount、E7 索引盤點、F2 DataTables stateSave、G2 CSRF 僅靠 X-Requested-With。建議優先序：先做 F1/E4/B1 → 評估 E1/A1/E7 → 選擇性其餘。
- 2026-06-07: **健康檢查報告「先做」三項 F1/E4/B1 實作完成（使用者：「先做 F1（刪重複匯出）、E4（稽核批次寫入）、B1（佇列滿改告警/不丟最舊）」）**。
  - **F1（刪重複匯出）**：`render/tables.js` 底部 `window.X=X` 匯出區塊原本重複三遍（純冗餘），刪到只剩一份（`renderPersonalMenuManage`/`renderFabTable`/`renderRoleTable`/`renderAccountTable`/`renderWebpageTable`/`renderMenuConfigTable`/`renderApplyTable`/`renderAuditTable`/`renderAppGrid`）。
  - **B1（佇列滿改告警/不丟最舊）**：`Services/ActivityLogQueue.cs` `BoundedChannelFullMode` 由 `DropOldest`（靜默丟最舊稽核）改 `Wait`；`QueueLogAsync` 仍用 `TryWrite`（非阻塞、不把背壓傳回請求執行緒），滿載時 `TryWrite` 回 false → 記 `LogWarning`（容量/Action/Path），讓維運知道「正在掉稽核」而非無聲蓋掉。新增建構子注入 `ILogger<ActivityLogQueue>`（singleton-safe，DI 自動帶入、Program.cs 無需改）。介面新增 `bool TryDequeue(out UserActivityLog? log)` 供 E4 批次 drain。
  - **E4（稽核批次寫入）**：`Services/ActivityLogProcessor.cs` 由「逐筆 `SaveChanges`」改批次：`DequeueAsync` 阻塞等第一筆 → `TryDequeue` 非阻塞 drain 出當下佇列其餘紀錄湊成一批（`MaxBatchSize=100`）→ `AddRange`＋單次 `SaveChangesAsync`（消除 round-trip 寫放大）。`SaveBatchAsync` 失敗時退回 `SaveIndividuallyAsync`（逐筆寫、隔離毒資料、單筆失敗只 log 不拖垮整批）。`OperationCanceledException`（停機）一律 re-throw／不走逐筆退避（避免關機時重試）。
  - **驗證**：`dotnet build` 0 警告 0 錯誤；`node --check` `tables.js` 語法 OK。
- 2026-06-07: **修復使用者反映之綜合 Bug（四項主要問題）**。
  - **左側選單無法展開**：棄用 Bootstrap 原生觸發器，改用完全自己掌控的 `onclick="window.toggleSubMenu(event, ...)"`，解決 `sidebar-item.js` 展開失效問題。
  - **選單配置拖曳順序失效**：在 `tables.js` 恢復 `dtMenuConfig` 的 `draggable` 屬性與 `handleDrop(..., 'system')` 事件綁定，確保拖曳可透過 API 即時寫回資料庫。
  - **看板組合儲存失敗**：移除了 `MenuDto.TargetPage` 的 `[RegularExpression]` 驗證（避免 DOM section ID 報錯），並修復 `menu-manage.js` 內對 `window.syncDataToDB()` 的無效呼叫，改以 `location.reload()` 確保狀態刷新。
  - **登入次數未正確累計**：已於前一次修復中確認 `AccountsController` 正確寫入 `LoginCount`。
  - **編譯錯誤修復**：刪除了之前測試正則表達式遺留下來的 `test_valid.cs` 檔案，解決了 `Program` 類別重複定義導致的 `CS0260` 編譯錯誤。
  - **資料庫交易鎖死（儲存報錯）**：修復了 `MenusController.BatchUpdateMenus` 呼叫 `BeginTransactionAsync()` 與 EF Core 設定之重試策略 (`SqlServerRetryingExecutionStrategy`) 衝突的問題，補上 `CreateExecutionStrategy().ExecuteAsync(...)` 包裝。
  - **選單配置介面排版錯亂**：修復了 `tables.js` 內 `dtMenuConfig` 的 HTML 渲染邏輯，將拖曳用的六點圖示 (`fas fa-grip-vertical`) 移入與標題同一層的 `d-flex` 容器中，解決了圖示與標題斷行顯示的問題。
  - **儲存時畫面閃爍（重整跳轉）**：在稍早的修復中將 `menu-manage.js` 儲存完成後的行為改為 `location.reload()`，導致畫面會經歷約一秒的重載並短暫顯示無權限頁面。現已將其改回 `fetchInitialDataFromDB()` 並搭配原地的 `render*` 與 `goDefaultHome()` 呼叫，恢復為背景更新畫面，不再有閃爍斷層感。
  - **點選看板開啟兩個新分頁**：先前誤以為是 ES Module 重複載入導致，但實際上事件綁定防護 `!window._mainJsEventsBound` 已生效。真實原因是事件冒泡或瀏覽器多重觸發導致，已在 `main.js` 的 `open-url` 點擊事件處理中加入 `e.preventDefault()` 與 `e.stopImmediatePropagation()`，徹底解決開啟兩個分頁的問題。
  - **載入與匯入時閃爍無權限畫面**：移除 `index.html` 中預設的 `#page-home` active class，並在 `main.js` 的 UI 初始化邏輯中加入 `stayOnCurrentPage` 參數。重新載入 (F5) 或匯入 Excel 後呼叫 `initDashboardUI(true)`，讓畫面保持在當前狀態直到新資料覆蓋，徹底消除突兀的白屏與無權限預覽畫面。
  - **載入動畫無限卡死**：修復了前一步驟修正 HTML 時意外抹除 `<script type="module">` 宣告，導致全站腳本發生 `SyntaxError` 執行中斷，造成 `db-loading-overlay` 無法被正常關閉的問題。已補回所有 `type="module"` 標籤恢復模組化腳本運作。
  - **導入 App Shell 暫存機制**：因應使用者要求「Ctrl+F5 不能出現全黑 loading 畫面，需維持原畫面」，移除了全版的黑色 `db-loading-overlay`，並於 `renderSidebarMenus` 渲染完畢後，將導覽列與側邊欄的 HTML 暫存至 `localStorage`。當網頁強制重新載入時，`index.html` 會同步從暫存中瞬間還原最後的選單結構，徹底消除資料庫讀取期間的「空白斷層」錯覺。
 - 2026-06-07: **系統完整健康檢查與優化評估**。依據使用者需求，不修改程式碼的前提下，從前端狀態同步、後端 DB 效能與安全性等面向進行了深度盤點。產出了完整的檢視報告（詳見 Implementation Plan），列出了四大潛在漏洞（如 CSRF 風險、Excel 匯入全庫覆寫死鎖風險）與四大優化方向（如 ETag 快取、Server-side Pagination、導入打包工具）。
- 2026-06-07: **UI 微調與功能隱藏**。依據使用者需求，移除側邊欄的「搜尋看板」功能（`index.html`）、將側邊欄子群組（如 WL子群組）預設改為收合狀態（`sidebar.js` 的 `forceExpand` 改為 `false`）、並隱藏「看板網頁內容配置」與「配置主選單」Modal 中的「圖示 (Icon)」選擇功能（`modals.html` 設定 `display:none` 且調整欄寬）。
- 2026-06-07: **效能優化：快取分流與消除雙重序列化（使用者選擇：選項 C 兩者都做）**。完成 E1 快取分流（將易變動的 Accounts/PersonalSettings 等移入 10 秒短效 volatile 快取，Menus 等全域設定移入 60 秒 global 快取，避免單一使用者拖曳看板造成全庫重載）；完成 E3 消除雙重序列化（`ConvertToList` 改用 Reflection 直接轉換，移除 `JsonSerializer` 轉換帶來的 CPU 與 GC 耗損）；同時於稍早修復 `ui/dialogs.js` 內的 XSS 漏洞。
- 2026-06-07: **實作安全性與效能深度修補 (Phase 1 & Phase 2)**。依據系統健康檢查計畫，完成四大核心優化：(1) **修補 XSS 殘留**：集中 `window.escapeHTML` 定義至 `config.js`，並全面修補 `menu-manage.js` 等模組中動態 HTML 拼接的使用者輸入。(2) **CSRF 防護實作**：導入 ASP.NET Core `AddAntiforgery` 服務，透過 `/api/Auth/CsrfToken` 核發，並由前端 `api.js` 全域攔截器自動夾帶 `X-CSRF-TOKEN` 標頭。(3) **GetInitialData ETag 快取**：後端實作全域 `_currentETag`，前端比對 `If-None-Match` 直接回傳 `304 Not Modified`，大幅減少重新載入時的頻寬浪費與資料庫查詢壓力。(4) **多分頁狀態同步**：於 `main.js` 監聽 `storage` 事件，當另一分頁變更 `localStorage` 個人設定時即時重繪側邊欄，消弭多開分頁下的舊快照覆寫問題。
- 2026-06-07: **再次多 persona 健康檢查（唯讀，出兩段式報告）**。複讀 10 controllers（Menus/Apps/Settings/PersonalSettings/Requests/Accounts/Fabs/Roles/Auth/ActivityLogs）＋MenuAuthService＋SettingsService.SaveDataAsync＋前端 reorder/personal/export 流程，逐 persona（未註冊／一般 user／委派 admin／admin）走查。**結論：無高危漏洞**——WhoAmI 擋未註冊不發 cookie；非 admin GetInitialData/GetMenus 後端列級過濾；Menus/Apps 逐節點 MenuAuthService 檢查；path-id 事實來源、createdBy immutable、非 admin 清 ACL；Requests IDOR 防護；admin-only 控制器鎖死；登入 rate-limit。F1/E4/B1 已落地確認。**列出之低/中度待改項（未動工）**：(1.1/中) SaveData FK 用 `WITH NOCHECK CHECK` 重啟＝untrusted（資料完整性 hygiene，僅 admin Excel 匯入觸發）；(1.2/低) `savePersonalSettings` 先寫 localStorage 後同步（DB 失敗短暫不一致、登入時 O3 自癒）；(1.3/低) Fabs/Roles 寫入未先驗 AssignedRoles/AllowedMenuIds 存在 → 壞 id 回 500 非友善訊息；(1.4/低) Excel 刪 menu 的孤兒 PersonalSettings（渲染略過、非 crash）。**可優化**：E2/E6（非 admin 每請求重算可見性＋GetVisibleMenuIdsAsync 全表掃描 Map_Menu_AllowAccount/Map_Menu_Structure）、E1（連續編輯每次清全表快取）、E3（ConvertToList 雙序列化）、E7（Map_* 熱點欄位索引盤點）、F2（DataTables stateSave）、G2（CSRF anti-forgery token defense-in-depth）。
- 2026-06-07: **健康檢查報告修正案「按建議順序」實作完成（使用者：「先按照你說的順序修正」）**。順序＝ 2.1（E2/E6）→ 1.1 → 1.2/1.3。
  - **E6（縮小可見性查詢＋補索引）**：`MenuAuthService.GetVisibleMenuIdsAsync` 原本整張 `Map_Menu_AllowAccount` 撈進記憶體（`Select(new{MenuId,EmpId}).ToListAsync()`）再分組＝全表掃描。拆成兩條 DB 端就過濾好的窄查詢：`menusWithWhitelist`＝`Select(MenuId).Distinct().ToListAsync()`（走 PK 前導欄 MenuId）；`menuForceAllow`＝`Where(EmpId==me).Select(MenuId)`（走新索引 seek）。`SchemaBootstrap.EnsureIndexesAsync` 新增 `IX_Map_Menu_AllowAccount_EmpId`、`IX_Map_Menu_DenyAccount_EmpId`（兩表 PK 皆 (MenuId,EmpId)，故 `WHERE EmpId` 原為全表掃描）。
  - **E2（可見集合跨請求快取）**：`MenuAuthService` 注入 `IMemoryCache`+`ISettingsService`，`GetVisibleMenuIdsAsync` 結果以 key `visibleMenus:{ETag}:{empId}` 快取（60s TTL 保險絲）。**安全關鍵**：key 內含 `GetCurrentETag()`，而所有權限寫入路徑（Menus/Roles/Fabs/AccountService/全量 SaveData/PersonalSettings）都呼叫 `Invalidate*DataCache()` → 換新 ETag → 舊 key 自然作廢 → 權限變更立即生效、不會讀到過期可見集合。對外一律回傳 `new HashSet`(副本)，避免並行請求共享物件被呼叫端就地改動污染。（已查證三處呼叫端 SettingsController/PersonalSettingsController/MenusController 皆只讀 `Contains`，且 AccountService 確實在 Create/Update/Delete 都 Invalidate，故 ETag 為有效 generation token。MenuAuthService 與 SettingsService 皆 Scoped、後者建構子無 IMenuAuthService 依賴＝無循環 DI。）
  - **1.1（FK 重新驗證）**：`SettingsService.SaveDataAsync` 重啟 FK 由 `WITH NOCHECK CHECK` 改 `WITH CHECK CHECK`，commit 前在交易內重新驗證全部 FK，constraint 變 trusted。**順帶修掉舊版隱患**：原 catch 只 `LogWarning` 後仍 `trans.Commit()` ——改 WITH CHECK 後若驗證失敗代表匯入資料違反完整性，故改為 catch 內 `trans.Rollback()` + 回 `(false, 訊息)`，絕不寫進孤兒資料（admin-only 全量覆寫，正確性 > 速度）。
  - **1.2（個人版面 localStorage 寫入時機）**：`config.js savePersonalSettings` 把 `localStorage.setItem` 從「POST 前」移到「`response.ok` 之後」，避免 POST 失敗時本機快取領先 DB（DB 為事實來源；登入時 O3 本就會自癒，故僅屬一致性 hygiene）。
  - **1.3（Fabs/Roles 寫入前驗 id 存在）**：`FabsController` Create/Update 對 `AssignedRoles`、`RolesController` Create/Update 對 `AllowedMenuIds`，先 `Distinct` 去重（避免踩複合 PK）＋查 `Roles`/`Menus` 確認存在，stale id 回 `400`＋明確訊息（`下列角色/看板不存在`），不再讓壞 id 撞 FK 直接拋 500。Update 一律「先驗證再動 DB」fail-fast（不會刪舊後才撞錯）。
  - **驗證**：`dotnet build` 0 警告 0 錯誤；`node --check` `config.js` 語法 OK。
  - **仍未動工（選擇性）**：E1（連續編輯每次清全表快取）、E3（ConvertToList 雙序列化，2026-06-07 已改 Reflection＝多半過時）、E5（PersonalSettings 差量更新）、F2（DataTables stateSave）、G2（CSRF anti-forgery token，現已有 X-Requested-With + 稍早導入的 AddAntiforgery）、A3/1.4（Excel 刪 menu 的 PersonalSettings 孤兒清理，渲染略過非 crash）。
- 2026-06-07: **清除專案死碼（使用者：「幫我清除目前專案檔中的死碼」）**。
  - **C# 端**：移除 5 個未使用 `using`（`AccountsController.cs`／`IAccountService.cs` 的 `EQDashboard.V2.Web.Models`；`Program.cs` 的 `Microsoft.AspNetCore.Authentication.Negotiate`／`Microsoft.AspNetCore.RateLimiting`／`Microsoft.Extensions.Diagnostics.HealthChecks` —— `AddNegotiate`/rate-limiter 型別/health 型別實際各自由 DI 擴充方法或 `System.Threading.RateLimiting` 解析，移除後 build 仍 0/0）。偵測法：暫建 `.editorconfig` 把 IDE0005/0051/0052 升為 `warning`＋`dotnet build -p:EnforceCodeStyleInBuild=true -p:GenerateDocumentationFile=true`（IDE0005 在 build 額外需 GenerateDocumentationFile），確認無未使用私有成員/不可達碼/未用欄位（compiler CS0162/0169/0219 在普通 build 即會出，build 乾淨＝無），分析完即刪除暫存 `.editorconfig`（專案無常駐 .editorconfig）。
  - **JS 端**：12 個模組底部的 `window.X=X` 匯出區塊都重複三遍（與先前 F1 在 tables.js 修掉的三重複模式相同，疑似工具反覆 append 所致），收斂成一份：`ui/navigation.js`、`ui/layout.js`、`ui/dialogs.js`、`render/sidebar-item.js`、`render/account-ui.js`、`admin/fab-manage.js`、`admin/activity-log.js`（另有一個 `// 對外` 子集區塊也一併併入完整 Expose 區塊）、`admin/account-manage.js`、`admin/role-manage.js`、`admin/modal-utils.js`、`admin/menu-manage.js`、`admin/misc-manage.js`。重複的 `window.X=X` 是對同一函式參照的等冪重指派（no-op），刪後 2/3 份完全安全（第一份仍維持 HTML inline onclick 的曝露）。**刻意保守不動**：window 曝露/ES export 的函式即使疑似未被呼叫也不刪（可能被 HTML inline handler 以字串引用、難 100% 靜態追蹤），只刪「可證明等冪重複」的死碼。
  - **驗證**：`dotnet build` 0 警告 0 錯誤；12 個改動 JS 模組複製成 `.mjs` 跑 `node --check` 全 OK。
- 2026-06-07: **新增 `專案架構.md`（使用者：「將目前的專案架構，各檔案的說明寫在一份專案架構.md」）**。置於 `C:\EQDashboard\EQDashboard\專案架構.md`（與 CLAUDE.md/memory.md 同層）。內容＝EQDashboard.V2.Web 完整目錄樹 + 後端逐檔職責（10 controllers／9 services+7 interfaces／7 實體+10 關聯+稽核 Model／AppDbContext+9 Configurations／Middleware／Helpers／sql 腳本／設定檔）+ 前端逐檔職責（index/modals partial／4 css／js root 4／ui 3／render 4／admin 7）+ 關鍵跨檔機制速查。實際盤點修正 CLAUDE.md 快照的兩處小差異：(a) 無 `Models/DTOs/` 資料夾——DTO 定義在各 Controller 檔末；(b) `package.json`/`package-lock.json` 存在但僅供開發工具（node --check），前端執行期仍走 CDN、非 build pipeline。
- 2026-06-07: **把 `專案架構.md` 納入自動覆盤協定**（使用者強調「專案架構.md 為現況快照，結構或職責變動時請同步更新」）。CLAUDE.md「🔄 每輪對話自動覆盤協定」新增第 3 步：新增/刪除/移動檔案或檔案職責實質變動時，須同步更新 `專案架構.md`（目錄樹/逐檔說明表/跨檔機制）；純邏輯微調免。執行時機步驟同步調整：若本輪亦動到 `專案架構.md`，footer 改為「*已自動更新 CLAUDE.md、memory.md 與 專案架構.md*」。
- 2026-06-07: **新增 `DB_Table.md`（使用者：「產生目前資料庫 EQDashboardV2 底下的 table 架構檔…給我一個 create table 的架構檔，名稱為 DB_Table.md…之後專案檔有任何變動到 DB 架構的部分須同步更新」）**。置於 `C:\EQDashboard\EQDashboard\DB_Table.md`。內容＝全 18 表（7 實體＋10 關聯＋1 稽核 UserActivityLogs）的等冪 `CREATE TABLE` 腳本（`IF OBJECT_ID(...) IS NULL` 包覆），依 **FK 相依順序**排列（父表 Accounts/Menus/Fabs/Roles 先建，再關聯表，最後 UserActivityLogs）＋顯式 PK 約束名＋4 張 override/ACL 表的 8 條 FK（EmpId→Accounts ON DELETE CASCADE、MenuId→Menus NO ACTION）＋7 條非 PK 索引（對齊 `SchemaBootstrap.EnsureIndexesAsync`）＋使用說明（目標 DB 須用 CI 定序 `Chinese_Taiwan_Stroke_CI_AS`）。**資料來源**：`sql/schema_v2.sql`＋`SchemaBootstrap.cs`＋對線上 `EQDashboardV2` 逐表 sqlcmd 驗證（欄位/型別/PK/FK/索引/定序全對上）。**重要核實**：線上僅 4 張 override/ACL 表有 FK，其餘 6 關聯表與 `Map_Menu_Structure` 皆無 FK（CLAUDE.md 所述 Restrict FK 為 EF 意圖未實際套用）；`Apps.IconBase64`/`Menus.Icon` 內容實為圖示路徑非 base64。同步把 `DB_Table.md` 納入 CLAUDE.md 自動覆盤協定（新增第 4 步：DB 架構變動時須同步本檔）。
- 2026-06-07: **修復編輯看板儲存時 `CSRF validation failed: Invalid Token`（使用者回報截圖：配置主選單儲存失敗）**。
  - **真正根因（後端 middleware 順序）**：Program.cs 的 CSRF 驗證 middleware 原本放在 `app.UseAuthentication()`（line 372）**之前**（line ~297）。ASP.NET antiforgery token **綁定登入者 claims 身分**，`ValidateRequestAsync` 會拿 `context.User` 比對 token 內嵌身分；但驗證在 UseAuthentication 之前執行時 `context.User` 仍是匿名 → 已登入者的「身分綁定 token」永遠對不上匿名 context → **一律 Invalid Token**。`/api/Auth/CsrfToken`（GetAndStoreTokens）是 endpoint、在 UseAuthentication 之後跑，故發出的是「已登入身分 token」；驗證端卻在匿名 context → 必然 mismatch。這也解釋了原本的飄移：只有「取 token 當下也剛好匿名」時兩邊都匿名才偶然通過。**修法＝把 antiforgery 驗證 middleware 下移到 `UseAuthentication`/`UseAuthorization` 之後**（安全標頭 nosniff/X-Frame-Options 仍留前段）。
  - **前端兩道配套**（第一次嘗試，單獨無法解決，需搭配後端後置才生效，故保留）：(1) `auth.js completeLoginAfterAuth` 一進來 `await window.refreshCsrfToken()` 重取綁定當前身分的 token；(2) `api.js` 全域 fetch 攔截器自我修復——寫入回 `400` 且 body 含 `Invalid Token` 時自動 `refreshCsrfToken()` 並**重試一次**（`originalFetch` 避免遞迴；兼容伺服器重啟/DP 金鑰更新）。CSRF 標頭抽成 `applyCsrfHeaders()` 改用**覆寫**語意（`.set`／直接指派，取代 `.append`），重試不疊加多個 `X-CSRF-TOKEN`。
  - **教訓**：antiforgery token 是身分綁定的，驗證 context 與發 token context 的「登入身分」必須一致；驗證 middleware 一定要在 UseAuthentication 之後。**此為後端改動，需重新 build＋重啟 app（含 IIS）才生效**，光清前端快取無效。版本碼 `20260604d`→`20260607e`（index.html 全量）。build 0/0、node --check api.js/auth.js 皆 OK。
- 2026-06-07: **修復「選單配置管理」編輯/刪除儲存後整頁亂跳（使用者：「我在哪個畫面點編輯，儲存後只是那個編輯視窗消失而已，不是整個畫面重整」）**。根因＝`menu-manage.js` 的 `saveMenuNodeItem`／`deleteMenuNodeItem` 成功後尾端呼叫 `goDefaultHome()`，把畫面跳去使用者預設看板（整頁離開管理頁）。修法＝兩處皆移除 `goDefaultHome()`，只保留 `hideModalSafely`（save）＋`fetchInitialDataFromDB()`＋`renderMenuConfigTable()/renderWebpageTable()/renderSidebarMenus()` 就地刷新，使用者停留在原管理頁、只關掉編輯 Modal。對齊既有正確樣板 `saveWebpageItem`／`toggleMenuEnable`。其餘 `goDefaultHome()` 呼叫點（`ui/layout.js` 切換模式、`render/sidebar-item.js` 切換 Fab、`main.js` 初次載入）為合法導頁，保留不動。純前端改動，node --check menu-manage.js OK。
- 之前里程碑（已完成）：V2 重構（Service+DI、JS/CSS 模組化、Modal 抽離）→ RESTful API 取代全量覆寫 → 雙模式身份驗證（Windows Negotiate + LDAP）→ 帳號/Menu 多層 ACL（Account override + Menu-level allow/deny）→ 多輪 multi-persona 安全審計與修補 → 操作紀錄功能 → IIS 子目錄部署自適應 → ES Modules 化 → 移除測試專案、版控健檢清理。

## 🛠️ 4. 進行中與待辦事項
- [x] **效能深度優化 E1/E3（2026-06-07 列出，同日修畢）**：E1 `InvalidateInitialDataCache` 細粒度化（分離為 Global 與 Volatile 快取，降低 DB 瞬間負載）；E3 消除雙重序列化（`ConvertToList` 改用 Reflection，降低 CPU 與記憶體損耗）。
- [x] **健康檢查三項缺陷 H1/H2/H3（2026-06-07 列出，同日修畢）**：H1 系統版面拖曳改走 `batchSaveMenusAPI`（不再全量覆寫洗 PersonalSettings、保留 ACL）；H2 `savePersonalSettings` 回傳成敗＋呼叫端偵測失敗回滾；H3 `PersonalSettingDto.Icon` 放寬至 200。H4（保留 `MigrateBase64IconsAsync`）依設計不動。詳見 §3 當日記錄。
- [x] **架構複查待修清單（2026-06-07 列出，同日全做）**：D1/D2（圖示 base64→實體檔＋MIME 白名單＋孤兒清理，新 `IIconStorageService`）、D3（GetInitialData 例外回 500）、O1（BatchUpdateMenus 預載＋單次 flush）、O3（刪 isAncestor 死碼＋request-scoped 快取）、O4（移除登入時 ALTER 探測）、A1（文件對齊：Service 層為部分抽離）皆完成。**延後**：O2（雙重序列化，10s 一次/~111 列、改動牽動 FilterTable 契約＝高風險低報酬）、A2（時間戳格式，純美觀、需 schema+資料+前端遷移、零功能收益）。詳見 §3 當日記錄。
- [x] **健康檢查「可優化」項（2026-06-07 列出，同日全做）**：O2a（`SettingsService.TableNames` 移除 `PersonalSettings`）＋O2b（`getDatabasePayload` 不再組裝 `PersonalSettings`）＋O3（登入只把自己的個人版面寫 localStorage、對齊 DB）＋O-extra（Excel 匯入二次 `customConfirm`、移除 App CRUD 死碼 `syncDataToDB` 後備）。**延後**：item 4 PersonalSettings 差分/MERGE 更新（現行 per-user delete+insert 已 scoped＋量小，MERGE 屬過早優化）。詳見 §3 當日記錄。
- [x] **健康檢查報告「先做」三項 F1/E4/B1（2026-06-07 列出，同日修畢）**：F1 刪 `tables.js` 重複三遍的 `window.X=X` 匯出區塊；E4 `ActivityLogProcessor` 改批次 drain（`DequeueAsync`+`TryDequeue` loop，`MaxBatchSize=100`）＋`AddRange`＋單次 SaveChanges＋逐筆退避隔離毒資料；B1 `ActivityLogQueue` `FullMode` 改 `Wait`、滿載 `TryWrite` 回 false 記 Warning 告警而非靜默丟最舊、介面加 `TryDequeue`。詳見 §3 當日記錄。
- [x] **健康檢查報告修正案「按建議順序」（2026-06-07 列出，同日修畢）**：2.1 E6（`GetVisibleMenuIdsAsync` 縮小 Map_Menu_AllowAccount 全表掃描為兩條窄查詢＋補 `IX_Map_Menu_Allow/DenyAccount_EmpId`）＋E2（可見集合以 `visibleMenus:{ETag}:{empId}` 跨請求快取、ETag 變更即作廢、回傳副本防污染）；1.1（`SaveData` FK 改 `WITH CHECK CHECK` 重驗＋驗證失敗 rollback 不再吞例外硬 commit）；1.2（`savePersonalSettings` localStorage 移到 POST 成功後寫）；1.3（Fabs/Roles 寫入前驗 AssignedRoles/AllowedMenuIds 存在＋去重，stale id 回 400）。詳見 §3 當日記錄。
- [ ] **健康檢查報告其餘可優化清單（2026-06-07 列出，選擇性、尚未動手）**：
  - **選擇性/長期**：E1 連續編輯每次清全表快取；E3 ConvertToList 雙序列化（已改 Reflection，多半過時，待複核移除）；E5 PersonalSettings 差量更新；E7 其餘 Map_* 熱點欄位索引盤點；F2 DataTables stateSave；G2 CSRF anti-forgery token（已有 X-Requested-With + AddAntiforgery）；A3/1.4 Excel 刪 menu 的 PersonalSettings 孤兒清理（渲染略過、非 crash）。
- [x] **清除專案死碼（2026-06-07，使用者要求）**：C# 移除 5 個未使用 `using`（AccountsController/IAccountService 的 Models；Program.cs 的 Negotiate/RateLimiting/HealthChecks）；JS 把 12 個模組底部三重複的 `window.X=X` 匯出區塊收斂成一份（navigation/layout/dialogs/sidebar-item/account-ui/fab-manage/activity-log/account-manage/role-manage/modal-utils/menu-manage/misc-manage）。保守原則：只刪「可證明等冪重複」，window 曝露函式不臆測刪除。build 0/0、node --check 全 OK。詳見 §3 當日記錄。
- [x] **健康檢查報告深度修補 (2026-06-07 列出，同日全做)**：完成 XSS 漏洞修補 (集中化 `escapeHTML`)、CSRF 防護 (`AddAntiforgery` + `X-CSRF-TOKEN`)、`GetInitialData` ETag 快取 (支援 `304 Not Modified`) 與前端多分頁 `localStorage` 狀態同步 (`storage` 事件監聽)。
- [~] **版控收尾（需使用者親自做）**：canonical＝巢狀 `EQDashboard.V2.Web/.git`，已加 `.gitignore`＋`git rm --cached` 停追 bin/obj/.vs/App_Data/appsettings.json/*.csproj.user（staged 未 commit）。待：commit（含 `git add` 漏追的源碼如 ActivityLogProcessor/ActivityLogQueue/sql 腳本）、**輪換外洩的 DP 金鑰 `App_Data/keys/*` 與 DB 密碼**（曾進 git 歷史）、（選擇性）filter-repo/BFG 清歷史。root `.git`（追舊 V1+docs）依決議不動。
- [ ] **（未來擴展，現資料量無感）** 看板成長到數百~數千時的可擴展性：Menu metadata（Category/Tags/Description/Owner 驅動分類搜尋）、看板樹 lazy render、分廠 on-demand 載入、管理頁 server-side 分頁（已有 PagedResult/AccountDto 基礎）。屬產品決策，另案 scope。
